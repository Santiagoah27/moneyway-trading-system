namespace MoneyWay.Application.MarketData.Candles;

/// <summary>
/// Identifies which body edge is selected from a caller-provided structural turn.
/// </summary>
public enum StructuralTurnBodyCoordinateSide
{
    Lower = 0,
    Upper = 1
}
