namespace MoneyWay.Application.MarketData.PriceLevels;

/// <summary>Identifies the strategy-neutral side on which an absolute price level is reached.</summary>
public enum PriceLevelDirection
{
    Lower = 0,
    Upper = 1,
}
