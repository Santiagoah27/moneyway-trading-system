# MoneyWay Nasdaq changelog

Entries are ordered newest first. Classification statements in older entries record the evidence state at that audit point and are superseded by later entries when explicitly reconciled.

## nasdaq-0.1.0-draft

- [ADR 0010](../../decisions/0010-define-nasdaq-collision-structural-price-replay-input.md) defines the one missing human `StructuralPrice` assertion for an exact `NQ-Q-H4-008` collision. The existing reconstruction episode, validating/migration candle and candidate side bind it; the validating wick remains the deterministic `ProtectionAnchor`. Generic replay-input visibility, exact-price compatible duplicates and conflicting prices are specified, while source provenance stays with the human observation rather than an invented body-source candle. Explicit correction/supersession, missing/conflict evaluator mapping and the runtime pipeline remain open. `NQ-Q-H4-007`, ordinary ADR 0009 membership, `NQ-H4-001`, metadata and capability are unchanged; documentation only.
- Architectural audit confirms that ADR 0009's exact rebuilt-episode key is stronger than the current post-invalidation state chain. `NasdaqHumanOriginVertexObservation` and its episode carry strategy/version, but `NasdaqHumanOriginVertexMemberResolution` drops both; opposite impulse, correction, candidate, rebuild pending, rebuilt tracking and breakout consequently retain only market/structural identity. The existing immutable `NasdaqHumanOriginVertexEpisode` is the required stable reconstruction identity and must be propagated unchanged through those states; migration and candidate side then complete the rebuilt-episode key. Ambient context, Nasdaq constants, latest-version lookup and defaults are prohibited substitutes. This is an architectural/runtime identity-contract gap only; strategy rules, `NQ-Q-H4-007`, `NQ-Q-H4-008`, metadata, capability and `NQ-H4-001` readiness remain unchanged.

- New human forensic review separates post-migration provisional turning from definitive HL/LH validation. The first closed bullish body for rebuilt HL or bearish body for rebuilt LH starts provisional tracking, including a migration candle with that same body role; exact doji does not. Every later closed H4 candle advances the market cursor whether selected for the final vertex or not. Strict new extremes reset the provisional turn, and only a later strict frozen-terminal breakout triggers retrospective definitive vertex classification. ADR 0009 human membership then supplies final geometry when causally visible. The existing complete `NasdaqPostInvalidationCandidateState` is not the pre-breakout rebuilt tracker; no runtime, metadata or capability changed.

- [ADR 0009](../../decisions/0009-define-nasdaq-rebuilt-candidate-vertex-replay-input.md) defines the positive human H4 member-selection input for one post-`CandidateProvisional` migration-only rebuild. Strategy/version, market series, original invalidating candle, strict migration candle and candidate side bind each rebuild episode; a later migration does not inherit earlier membership. The selected immutable member set includes the core migration candle, and deterministic calculators derive body/wick coordinates while checking the known migrated anchor. Generic `ObservedAtUtc`/`AsOfUtc` gating preserves causal replay. Compatible repeated sets and conflicting sets have explicit semantics; correction/supersession and missing-input evaluator mapping remain open. The immutable rebuild-pending state is the smallest next runtime unit and can precede the observation type. Documentation/ADR only; `NQ-Q-H4-008`, `NQ-H4-001`, metadata and capability remain unchanged.

- New human forensic review clarifies migration-only post-`CandidateProvisional` behavior: a strict deeper HL `Low`/higher LH `High` without second breakout supersedes the old candidate, discards its previous body geometry and failed zig-zag members, and reopens the correction around the new wick extreme. The final candidate comes from the deepest/highest turning region that launches the successful breakout impulse; its core extreme candle belongs, while exact adjacent H4 membership and thus rebuilt `StructuralPrice` still require human validation (`NQ-Q-H4-009`). A future intermediate rebuild-pending state must carry the known wick without inventing a complete geometry. This does not alter the separate same-candle `NQ-Q-H4-008` boundary. Documentation only; `NQ-H4-001`, runtime, metadata and capability remain unchanged.

- Documentation clarifies the complete six-row closed-H4-candle matrix after `CandidateProvisional`: provisional continuation, wick migration, ordinary validation for any body direction, directional migrate-then-validate, and the opposite-body/doji `NQ-Q-H4-008` boundary. The latter retains the confirmed breakout, new protection anchor and causal episode evidence while definitive `StructuralPrice` alone needs human resolution before pair completion. A future structural primitive can return this domain-level incomplete outcome before a distinct, `ObservedAtUtc`/`AsOfUtc`-gated human response contract is implemented; no new `RuleStatus` or runtime mapping is added. `NQ-H4-001` remains blocked by independent bootstrap, human evidence and orchestration gaps. Documentation only; metadata and capability unchanged.

- New human forensic review narrows `NQ-Q-H4-008`: the strict closed `Close > frozen provisional H` or `Close < frozen provisional L` confirms the breakout observation independently of bullish, bearish or exact-doji body color, even when `Open` gaps beyond the level; `Open` alone does not confirm it. If the same candle also makes a strict new Candidate HL `Low` or Candidate LH `High`, that wick replaces the prior `ProtectionAnchor` regardless of body color. The mentor does not define definitive `StructuralPrice` ownership for a contrary/doji-body validating candle that also migrates the wick: neither its body, the prior body coordinate nor body exclusion may be chosen automatically. The breakout/protection facts are deterministic; autonomous completion of the opposite pair remains human-validation blocked for this narrow case. The reviewed directional-body collision `NQ-Q-H4-007` stays resolved. Documentation only; no runtime, metadata or capability change, and `NQ-H4-001` remains blocked independently.

- Direct human review of Video 3 approximately `07:40–07:46` and Video 2 approximately `03:20–03:40` resolves `NQ-Q-H4-007` for the reviewed **directional-body** collision: when the same closed H4 candle both makes a strict new provisional candidate wick extreme and closes strictly beyond the frozen opposite-impulse terminal, migrate the candidate **before** validating the second break. The prior provisional candidate remains historical evidence; the confirming candle itself becomes the definitive HL/LH vertex. The reviewed bullish-body HL candle uses `min(Open, Close) = Open` and `Low` with new HH; the bearish-body LH mirror uses `max(Open, Close) = Open` and `High` with new LL. Both body and wick of this validating collision candle contribute, unlike the earlier first opposite-body candle's provisional snapshot, whose body is excluded. Ordinary non-migrating confirmation validates accumulated geometry. Incremental and retrospective causal results must agree in both specified paths. Because a strict validating Close does not itself guarantee the matching body direction after a gap, definitive-vertex precedence for the narrower contrary/doji-body collision remains `NQ-Q-H4-008 = UNRESOLVED`. Documentation only; runtime, metadata and capability remain unchanged, and `NQ-H4-001` still has independent blockers.

- Direct forensic review of Video 2 approximately `12:45–13:45`, `03:05–03:40` and `04:05–05:05`, with Video 3 approximately `07:40–07:46`, resolves the former provisional-candidate exceed question `NQ-Q-H4-006`: HL migrates on strict `Low < current Candidate ProtectionAnchor`; LH migrates on strict `High > current Candidate ProtectionAnchor`. Equality does not migrate, and the comparison uses wick protection rather than body coordinate or Close. The correction remains structurally open through zig-zags; the existing `CandidateProvisional` runtime snapshot is an incremental technique, not a finalized mentor HL/LH. Definitive correction geometry is selected at the later strict frozen-terminal body-close break and an incremental tracker must match that causal retrospective selection. A new-extreme candle may also start the next impulse through its body. Same-candle migration plus second-break confirmation is OHLC-representable but its precedence and final component membership remain `UNRESOLVED` as `NQ-Q-H4-007`. Documentation only; no runtime, metadata or capability change, and `NQ-H4-001` remains blocked.

- Direct human review of Video 2 approximately `10:20–10:35`, `03:05–03:40`, `07:25`, `01:40–01:55` and `13:20` separates post-invalidation candidate formation from later structural validation. The first closed body opposite to the active correction forms provisional LH/HL and begins the next impulse. Its body is outside candidate geometry, while an extending High/Low wick may update candidate protection; existing one-list geometry composition does not express this split. A later strict break of the frozen provisional terminal validates the candidate and new LL/HH. Exceeding a provisional candidate before validation conceptually discards it and resumes/rebuilds correction, but the exact observable exceed rule remains `UNRESOLVED` (`NQ-Q-H4-006`). Exact doji is neutral for formation; broader near-doji remains open. Documentation only; `NQ-H4-001`, metadata and capability remain unchanged.

- Post-invalidation lifecycle audit confirms that a valid same-episode human origin vertex plus the invalidating candle forms an immediately observable opposite H-to-L/L-to-H vector. The invalidating candle alone supplies the initial provisional terminal body edge and wick anchor; no opposite pair or candidate exists yet. `AwaitingCorrectionStart` requires a still-valid structural side and cannot directly represent this unvalidated opposite impulse. A small immutable handoff bound to the invalidation must carry origin, evolving terminal, opposite side and last processed candle; existing correction-start observation/classification can be reused afterward, while the lifecycle start overload cannot consume old-side `StructureInvalidated` directly. The frozen terminal body edge remains the later second-break reference. Documentation only; `NQ-H4-001`, metadata and capability remain unchanged.

- [ADR 0008](../../decisions/0008-define-nasdaq-human-origin-vertex-conflict-policy.md) confirms episode-scoped exact-set semantics for human origin-vertex observations. Repeated identical memberships are compatible and preserve all provenance; differing visible memberships block structural progression and map to `human_validation_required` without an automatic winner. `ObservedAtUtc` affects visibility only, so later evidence cannot rewrite earlier frames. Canonical transport and exact one-observation member resolution exist; the selector, explicit supersession and missing-evidence runtime status remain open. `NQ-H4-001` is not evaluator-ready. Documentation only; no metadata, capability or runtime change.

- [ADR 0007](../../decisions/0007-define-nasdaq-human-origin-vertex-replay-input.md) defines the positive, immutable human-reviewed post-invalidation origin-membership observation. Existing auxiliary-input identity, causal time and provenance combine with H4 invalidating-candle identity and exact selected candle identities; the verified transition supplies side and calculators derive coordinates. Membership is episode-bound and cross-session, with no future or invalidating candle in the vertex and no retrospective `AsOfUtc` rewrite. The single value-object contract is ready for implementation; canonical transport, duplicate/conflict and correction/supersession handling and missing-evidence runtime status remain open. `NQ-H4-001` is still not evaluator-ready. Documentation/ADR only; no runtime, metadata or capability change.

- Direct Video 2 approximately `11:10–11:35` and Video 3 approximately `07:25–07:38` H4/1H review classifies the post-invalidation origin's visual backward boundary and near-equal multi-candle grouping as human-validation required. Nearby bodies may form one turning zone without exact equal prices, but no numeric tolerance is source-backed. The final-cluster concept, extreme-candle inclusion, displacement exclusion and conditional body/wick formulas remain confirmed. Given reviewed members, subsequent structural reconstruction is deterministic; automatic vertex discovery and a single wick-extreme fallback are not canonical. A causal human-reviewed vertex replay observation is a possible later contract, separate from initial raw-history bootstrap. `NQ-Q-H4-005` now asks how that membership is supplied and validated. Documentation only; no runtime, metadata or capability change.

- Human review of Video 3 approximately `08:45–09:20` clarifies the 5M post-invalidation origin as the final turning cluster: the defining extreme belongs, an earlier advancing candle is outside, adjacent stagnation may belong, and the later displacement candle begins the opposite impulse outside the vertex. The H4 concept, forward boundary and supplied-member body/wick formulas remain confirmed; exact backward selection, near-equal membership and transfer of the detailed 5M criterion to H4 remain open. `NQ-Q-H4-005` is narrowed accordingly. Documentation only; no runtime, metadata or capability change.

- Further human review of Video 3 approximately `07:40–07:46` closes the post-invalidation corrective-body/new-extreme collision: the first qualifying opposite-body candle may extend the provisional terminal, contribute its body/wick geometry and enter the new correction on the same close. The terminal then freezes for the later strict body-close break. Video 3 approximately `08:45–09:15` and Video 2 approximately `11:10–11:35` also confirm that the departure origin may be a multi-candle vertex with separately aggregated body and wick coordinates, ending before the invalidating displacement candle. No exact first-candle boundary selects that turning cluster, so origin membership remains the sole open post-invalidation vector-construction input. Existing supplied-state geometry and correction-start primitives are compatible, but a complete replay handoff and `NQ-H4-001` evaluator are not ready. Documentation only; no runtime, metadata or capability change.

- Further direct human review of Video 2 approximately `05:45–06:45` and `11:10–11:45` and Video 3 approximately `07:25–09:20` confirms the invalidating candle as the first opposite-impulse member and the body-edge structural/wick-protection formulas for selected provisional vertices. Provisional L/H extends with later impulse extremes and freezes as the body-based second-break reference when correction begins; an immediate correction can leave the invalidating candle as terminal evidence. Exact origin-vertex candle selection remains open. The post-invalidation corrective-body/new-extreme collision is also unresolved: a valid-side same-candle update-and-start rule cannot be assumed to govern the provisional transition, and the supplied `Open = 101`, `Close = 100.5` example is bearish rather than the proposed bullish corrective body. Documentation only; no runtime, metadata or capability change.

- Direct human review of Video 2 approximately `09:20–10:55` and `12:10–12:40` clarifies post-invalidation strategy sequence. A strict closed break of validated HL/LH invalidates the former pair and immediately establishes opposite intent and a provisional bearish `H -> L` or bullish `L -> H` vector; an opposite correction supplies candidate LH/HL, and only a second strict closed break of the provisional L/H validates the new opposite pair. The first break does not validate opposite structure or require human raw-history rebootstrap. The invalidating candle belongs to the new impulse but is excluded from old candidate geometry. Exact provisional origin, terminal structural comparison coordinate and candle membership remain unspecified, so deterministic reconstruction is not implementation-ready. The transition stays causal and cross-session. Documentation only; no runtime, metadata, capability or ADR change.

- Direct human review of Video 2 approximately `05:00–05:10` and `09:55` and Video 3 approximately `06:15–06:35` confirms that valid H4 HH+HL or LL+LH state persists across trading sessions until qualifying structural evolution or invalidation. The daily 08:00 review compares the newly closed H4 candle with existing references; it does not rebootstrap raw history or require another human anchor solely because the date changed. Initial human selection remains auditable provenance as normal confirmed breaks roll the operational pair. Asia/London levels and preparation completion keep their separate session scope. Causal replay must expose persistent derived state without hidden evaluator instance state. Post-invalidation reconstruction, correction/conflict of human inputs and missing-anchor runtime status remain unresolved; documentation only, no ADR or runtime change.

- Human review of Video 2 approximately `05:45–06:10` and `11:10–11:35` and Video 3 approximately `07:20–07:46` resolves the two numeric coordinates and source-event identity of a human-selected initial H4 anchor. The selected High-side candle supplies body top `max(Open, Close)` for `StructuralPrice` and wick `High` for `ProtectionAnchor`; the Low-side mirror supplies body bottom `min(Open, Close)` and wick `Low`. One exact-price level may be shared by multiple candles while the human selects one source event as the causal initialization boundary; no first/last cluster rule is inferred. Anchor selection remains human, and input lifetime, reinitialization, conflicts and absent-input runtime status remain open. Documentation only; no replay input type, ADR, capability or evaluator was added.

- Further human review of Video 2 approximately `09:50–11:05` and the bullish post-anchor examples in Video 2 approximately `04:05–06:10` and Video 1 approximately `00:40–02:45` separates the confirmed visual origin of a dominant 4H leg from human-required selection of the first raw-candle anchor and initial scan boundary. No fixed lookback or viewport-based selector follows. Once valid references exist, the latest strict closed body break supplies direction and rolls the active HH+HL or LL+LH pair while preserving older audit history. Searching for that latest break cannot bootstrap raw history without the prior reference. `NQ-H4-001` remains unavailable as a fully deterministic raw-candle evaluator; this entry changes documentation only.

- Human runtime-state clarification completes the `NQ-TIME-003` status matrix: before 08:00 `not_applicable`; from 08:00 to before 08:30, `waiting` without a visible timely same-session completion or `passed` with one; at/after 08:30, `passed` if completion was timely or `failed` otherwise. Pre-08:00 and late assertions cannot satisfy the gate, later evidence cannot recover a missed deadline, and `AsOfUtc` prevents future evidence from changing earlier frames. The input observation and bounded replay transport are implemented; the evaluator and `NQ-LIQ-003` prerequisite remain absent. The strategy rule description still incorrectly states that replay context lacks the input; capability reason remains truthful. Older entries below record their historical audit state.

- [ADR 0006](../../decisions/0006-define-nasdaq-preparation-replay-input-contract.md) specifies a positive, source-backed completion observation for the exact Nasdaq session `D`, one causal UTC timestamp, `AsOfUtc` isolation, provenance, cross-session exclusion and one canonical assertion per session. It is a pre-evaluation auxiliary input owned by canonical replay and supplied through `StrategyReplayContext`, distinct from market-price and lifecycle-output evidence. The input primitive and evaluator remain unimplemented; `NQ-LIQ-003` still omits `NQ-TIME-003` from its prerequisite graph. The rule's name, required flag and confirmed metadata were reconciled in `f281497`, and its explicit `NotImplemented` capability reason in `4227dbc`; older entries below preserve their historical audit state.

- Human forensic review of Video 1 approximately `00:20–02:55`, Video 2 approximately `07:15–07:30`, Video 3 approximately `04:55–06:40`, and subsequent 08:30-boundary clarification confirms `NQ-TIME-003` preparation semantics. For the same morning session `D`, Step 1 closed-4H context/classification/bias and Step 2 Asia/London plus applicable 1H/4H liquidity marking must both actually complete in `[08:00:00, 08:30:00)` `America/Bogota`. 08:28 is an example, not a cutoff; 08:29 and 08:29:59.xxx remain eligible, while 08:30:00 begins Step 3 only for an already-prepared session. Missing the deadline makes `D` no-trade without late recovery, and completion expires with that session. Strategy content, completion, time boundary and session expiration are confirmed; the runtime completion-evidence contract remains unresolved. `NQ-TIME-003` is still optional and absent from the canonical Step-3 prerequisite graph, so this normative gate is not yet enforced. No runtime, rule metadata, capability, evaluator or workflow code changed.

- Direct human review of Video 2 approximately `01:40–01:55`, `03:05–03:40`, `04:45–05:25`, `05:40–06:15`, `06:40–07:35` and `10:55–11:05`, plus explicit subsequent human clarification, completes the supplied active-candidate boundary. Bearish `Close < prior LL` plus `Low < origin floor` confirms the old LH, excludes the confirming candle from its geometry, establishes the new LL-side extreme and rolls the active pair to new LL + LH, mirroring the confirmed bullish HH + HL collision. In both directions, an exact corrective-body confirming/extending candle starts the next correction as its sole initial member; other bodies leave the valid new side `AwaitingCorrectionStart` with an empty turn. Valid bullish `prior HL < prior HH` and bearish `prior LL < prior LH` ordering make strict confirmation plus opposite-point invalidation unreachable; wick penetration plus an invalidating close remains invalidation. `BEARISH_CANDIDATE_CONFIRMATION_AND_ORIGIN_RESET_PRECEDENCE` is `CONFIRMED`, `CONFIRMATION_RESET_CANDLE_NEXT_TURN_MEMBERSHIP` is `CONFIRMED_INCLUDED_WHEN_CORRECTIVE_BODY`, and `CANDIDATE_CONFIRMATION_AND_INVALIDATION_COLLISION` is `NOT_REACHABLE_IN_VALID_STRUCTURE`. The scoped `StructuralCandidateTurnBoundary` is ready for deterministic implementation with supplied valid references and an active turn; broader bootstrap and raw-history detection remain partial. No runtime, metadata, capabilities or workflow changed.

- Direct human review of Video 2 approximately `03:05–03:40` and `10:55`, followed by explicit human clarification, resolves the bullish same-candle `Close > prior structural HH` plus `High > current correction-origin ceiling` collision. Confirmation validates the old candidate HL and closes its turn successfully; the confirming candle is excluded from that HL's body/wick geometry while its new upper extreme establishes the new HH-side reference. The operational pair becomes new HH + validated HL at the causal close, with older structure retained for audit. Generic non-confirming reset/start/await and reset/invalidation precedence remain intact. `CANDIDATE_CONFIRMATION_AND_ORIGIN_RESET_PRECEDENCE`, `CONFIRMING_CANDLE_ESTABLISHES_NEW_EXTREME` and `CONFIRMED_CANDIDATE_ACTIVE_PAIR_ROLLOVER` are `CONFIRMED` for the bullish collision. `CONFIRMATION_RESET_CANDLE_NEXT_TURN_MEMBERSHIP` and the bearish confirmation-plus-extension mirror remain `UNRESOLVED`; the current specification also lacks an explicit ordered-reference invariant proving confirmation plus opposite-point invalidation impossible. No runtime, metadata, capability, evaluator or workflow artifact changed.

- Direct human review of Video 2 approximately `06:20–06:55` and Video 1 approximately `02:00–02:40` and `07:15–07:30` resolves active candidate-turn membership at the prior structural HH/LL validation reference. Equality and wick-only penetration are non-confirming and remain included, with OHLC eligible for selected-turn geometry and later deeper-HL/higher-LH replacement, absent an independent terminal event. A formally closed strict `Close > prior HH` or `Close < prior LL` validates causally and its candle is excluded. The prior structural validation reference is distinct from the current correction-origin ceiling/floor; strict origin-extreme reset and opposite-point invalidation retain precedence without inferred intrabar chronology. `PRIOR_HH_EQUALITY_MEMBERSHIP`, `PRIOR_HH_WICK_ONLY_PENETRATION_MEMBERSHIP`, `PRIOR_LL_EQUALITY_MEMBERSHIP`, `PRIOR_LL_WICK_ONLY_PENETRATION_MEMBERSHIP` and `PRIOR_HH_LL_NON_CONFIRMING_BOUNDARY_MEMBERSHIP` are `CONFIRMED_INCLUDED`; `CONFIRMING_BODY_CLOSE_CANDLE_MEMBERSHIP` is `CONFIRMED_EXCLUDED`. The prior-HH/LL inclusivity blocker is removed. Both correction windows, turn segmentation and structural detection remain `PARTIALLY_DEFINED` because near-doji treatment beyond exact comparisons, initial-anchor selection, historical scan termination and other structural coordinates remain incomplete. Runtime lacks an owner for automatic active-turn membership at this boundary; metadata and capability reasons still describe it as unresolved and require separate reconciliation.

- Semantic reconciliation distinguishes `ActiveCorrection`, `AwaitingCorrectionStart` and `StructureInvalidated` without choosing a C# representation. `ResetAndAwaitCorrectionStart` retains a valid structural side and permits a later `CorrectionStartTransition`; bullish/bearish invalidation destroys the former side and does not permit that transition until a valid new structural context exists. Continue and reset-and-start remain active, with the current candle included according to existing membership rules; reset/invalidation collisions remain invalidation-dominant. An empty/inactive turn alone is not evidence of waiting. Runtime `CorrectionTurnLifecycleResult` still collapses reset-and-await and invalidation into the same empty/inactive shape, so lifecycle integration remains blocked pending preservation of causal disposition. No runtime, metadata, capability or evaluator changed; bootstrap and broader turn-detection limits remain.

- Direct human review of Video 2 approximately `01:40–01:55` and `06:45` confirms that no correction turn exists before the first opposite-body candle. Every intervening same-direction body, exact doji and contained candle remains outside the future turn and its geometry, including after `ResetAndAwaitCorrectionStart`; a strict new High/Low extends the prior impulse price ceiling/floor without itself starting a correction or validating HH/LL. `PRE_START_WAITING_CANDLE_MEMBERSHIP = CONFIRMED_EXCLUDED_FROM_CORRECTION`. Exact `Close < Open` starts the bullish-structure correction and exact `Close > Open` starts the bearish-structure correction, with no minimum body-size rule. The full pre-start boundary and exact direction resolve the remaining start-specific questions, so `BULLISH_CORRECTION_START` moves from `PARTIALLY_DEFINED` to `CONFIRMED`; `BEARISH_CORRECTION_START` remains `CONFIRMED`. Both windows, turn segmentation and the global detector remain `PARTIALLY_DEFINED` because near-doji treatment beyond the exact start comparison, other inclusivity, initial-anchor and coordinate edges remain. Existing active-turn mixed-body membership and same-candle reset/start rules are unchanged. Current primitives compute body direction, price-extreme updates and active-turn reset/await, but lack a normal/pre-start waiting-to-first-opposite-body transition; no runtime, metadata, capability, evaluator, workflow or lifecycle artifact changed.

- Direct human review of Video 2 approximately `01:40–01:55`, `03:15–04:30` and `06:45` confirms the pre-start correction boundary. When the current HH/LL-producing candle is distinct from the first opposite-body candle, the extreme candle remains in the prior impulse and outside the new correction turn and its body/wick geometry. An exact-doji extreme candle before the first opposite body is likewise excluded. `DISTINCT_EXTREME_CANDLE_MEMBERSHIP = CONFIRMED_EXCLUDED_FROM_CORRECTION` and `PRE_START_EXACT_DOJI_AT_EXTREME = CONFIRMED_EXCLUDED_FROM_CORRECTION`. The correction still starts on the first bearish body in bullish structure or first bullish body in bearish structure; active-turn mixed-body continuity and same-candle reset-and-start inclusion are unchanged. `BULLISH_CORRECTION_START`, both correction windows, turn segmentation and overall detector retain their prior partial classifications; `BEARISH_CORRECTION_START` remains confirmed. Near-doji, near-equal tolerance, initial-anchor and historical scan boundaries, and other genuine inclusivity edges remain open. Existing primitives accept caller-selected turns and show no conflicting automatic inclusion; no runtime, metadata, capability, evaluator, workflow or lifecycle artifact changed.

- Direct human review of Video 2 approximately `06:30`, `11:10–11:25` and `12:20`, plus Video 3 approximately `07:25–07:35` and `09:00`, confirms exact equal-price structural identity. Multiple candles in the same selected turn whose body extrema produce the exact same structural coordinate support one structural price level/zone; they do not create multiple HL/LH points, and no first/last candle, timestamp or sequence tie-break is required. `STRUCTURAL_EQUAL_PRICE_IDENTITY = CONFIRMED_PRICE_LEVEL`, `STRUCTURAL_EQUAL_PRICE_CANDLE_TIE_BREAK = CONFIRMED_NOT_REQUIRED` and `STRUCTURAL_EQUAL_PRICE_SHARED_ZONE = CONFIRMED`. Lower/HL structure remains the minimum body edge with minimum turn `Low` as its separate protection anchor; upper/LH structure remains the maximum body edge with maximum turn `High` as its separate protection anchor. Different candles may supply those extrema without contradiction: `STRUCTURAL_PROTECTION_WICK_ACROSS_TURN = CONFIRMED`. “Zone” defines no numeric width, and `NEAR_EQUAL_STRUCTURAL_ZONE_TOLERANCE = UNRESOLVED`. Executable Stop Loss offset semantics remain unresolved. Existing body-coordinate, protection-anchor and composed-geometry primitives align with this evidence; no runtime, metadata, capability, evaluator, workflow or lifecycle artifact changed.

- Direct human review of Video 2 approximately `08:00–08:25`, `09:55–11:05` and `12:20–12:40`, plus Video 3 approximately `05:50–06:40` and `14:15–14:40`, narrows the 4H structural bootstrap boundary. The mentor defines no fixed candles, days, sessions, time window, scan count, pivot period or numeric lookback: `H4_FIXED_LOOKBACK_RULE = CONFIRMED_NONE`. The initial evident High/Low that originated the current directional leg is a confirmed visual anchor concept, but selecting it from arbitrary raw 4H candles remains `HUMAN_VALIDATION_REQUIRED`; viewport, zoom or loaded-history length cannot become hidden engine rules. Once valid structural references exist, the latest strict formally closed body break supplies inherited direction and the latest HH+HL or LL+LH becomes the operational active pair while older points remain auditable. Wick-only penetration and equality do not establish the break. Raw-candle initialization and full deterministic bootstrap remain blocked by first-anchor selection and historical scan termination. Weekly open, previous-day extrema and Asia/London levels remain separate context references, not mandatory structural seeds. No runtime, metadata, capability, primitive, evaluator, workflow or lifecycle artifact changed.

- Direct human review of Video 2 approximately `02:20–02:30`, supported by the closed-body evidence around `06:25–06:55`, confirms that opening/quotation gaps have no special structural role. `previous.Close != current.Open` alone does not start or reset a correction, invalidate structure, fragment an active turn, create a structural point or validate one, regardless of gap size. Exact body direction continues to govern correction start, strict High/Low extremes govern reset, and formally closed Close governs prior-HL/LH invalidation. No synthetic path, interpolation or candle is introduced across the missing interval, and an opening discontinuity is not FVG/IFVG or Step-4/Step-5/Step-6 evidence. `OPENING_GAP_STRUCTURAL_ROLE = CONFIRMED_NO_SPECIAL_ROLE`; gap correction-start, reset, invalidation and turn-membership classifications are confirmed as documented. Correction windows, turn segmentation and structural detection remain `PARTIALLY_DEFINED` because near-doji treatment, remaining inclusivity, bootstrap and structural identity/coordinate edges remain open. No runtime, metadata or capability declaration changed.

- New direct human review of Video 2 approximately `06:40` and `12:15–12:35`, Video 3 approximately `04:30`, and the reviewed bearish collision confirms exact causal mirror symmetry for active-correction terminal mechanics. In-range bullish, bearish and exact-doji candles remain in one bearish correction turn. A strict new Low resets the correction, updates its price floor and discards the candidate LH; a bullish reset candle becomes the first member of the new turn, while neutral and bearish reset candles start no new correction. A formally closed `Close > prior validated LH` invalidates bearish structure with zero tolerance; equality and wick-only penetration do not. The candle is excluded from the destroyed turn and candidate geometry and belongs to the new bullish impulse context. When strict reset and invalidation collide, invalidation dominates; the collision `Low` remains a lower price extreme/bullish-origin and protection reference without becoming a validated HL, Structural Low or executable Stop Loss. Earlier replay snapshots remain unchanged and no intrabar sequence is inferred. `BEARISH_RESET_INVALIDATION_SYMMETRY = CONFIRMED_MIRROR`; membership, reset, invalidation and collision classifications are confirmed as documented. The broader correction windows, turn segmentation and structural detector remain `PARTIALLY_DEFINED` because near-doji, gaps, remaining inclusivity, bootstrap and other identity/coordinate edges remain open.

- The reviewed bearish collision also supplies narrow `context_specific` evidence: the mentor calls the lower-wick/new-Low event a liquidity take and the body close above prior LH structural change, with the candle beginning a bullish impulse. This does not generalize every collision to `NQ-LIQ-003` or `NQ-M5-001` and does not bypass selected-liquidity, 4H alignment, timeframe or workflow prerequisites. The canonical progression fold uses prerequisites established before the current replay frame, so a future approved mapping of both gates to one causal frame exposes an unresolved same-frame gating discrepancy. No runtime, prerequisite graph, metadata or capability declaration changed.

- Direct human review of Video 2 approximately `08:50–10:15` and Video 3 approximately `08:50–09:15`, with context around Video 3 `07:25`, resolves the active-bullish-correction collision. When one formally closed candle has both `High > current correction-origin ceiling` and `Close < prior validated HL`, both observations are true but invalidation dominates the structural transition. The bullish candidate is destroyed; the candle is excluded from its old turn and geometry and belongs to the new bearish impulse context. Its `High` remains a relevant upper price extreme/bearish-origin reference without becoming an automatically validated LH, structural High or executable Stop Loss. The decision uses closed OHLC without synthetic intrabar chronology and affects state only from that causal `AsOfUtc`; prior replay snapshots remain unchanged. No Step-3/Step-4 `RuleId` mapping is inferred. `BULLISH_RESET_INVALIDATION_COLLISION_PRECEDENCE = CONFIRMED_INVALIDATION_DOMINATES`; collision membership and structural-change roles are confirmed as documented, while the broader structural states remain `PARTIALLY_DEFINED` and bearish symmetry remains `UNRESOLVED`.

- Latest direct human review confirms the remaining bullish terminal-candle membership semantics. A strict `High > current correction-origin ceiling` first resets the old correction, updates the ceiling and discards its candidate. If that same closed candle has `Close < Open`, it is excluded from the old turn and included as the first member of the new bullish-correction turn; neutral and bullish reset candles start no new correction. A formally closed `Close < prior validated HL` invalidation candle is excluded from the destroyed turn and candidate geometry and supplies new bearish-impulse evidence without automatically validating a structural LL. Membership is fixed at the causal closed boundary without synthetic intrabar chronology or later reinterpretation. The updated ceiling remains distinct from a validated structural HH. `BULLISH_NEW_HIGH_RESET_CANDLE_DUAL_ROLE`, `BULLISH_INVALIDATION_CANDLE_NEW_BEARISH_IMPULSE_ROLE` are `CONFIRMED`; reset/invalidation memberships are `CONFIRMED_INCLUDED` or `CONFIRMED_EXCLUDED` as applicable. `BULLISH_CORRECTION_WINDOW`, `CANDIDATE_HL_SELECTION`, `TURN_MEMBERSHIP_SEGMENTATION` and overall structural candidate detection remain `PARTIALLY_DEFINED`; bearish reset/invalidation symmetry remains `UNRESOLVED`.

- Direct human review of Video 2 approximately `09:25–10:15` and `12:45–13:40`, especially `13:00–13:10`, confirms bullish correction-turn continuity and two terminal/reset events. Once the correction is active, in-range bearish, bullish and exact-doji candles, internal oscillations and consolidations remain in the same turn; body-color alternation does not create parallel HL candidates. A strict `High > current correction-origin ceiling` resets the correction, updates that price ceiling and discards its current candidate without validating a structural HH. A formally closed `Close < prior validated HL` invalidates bullish structure; equality and wick-only penetration do not. Terminal/reset candle geometry membership and the symmetric bearish rules remain unresolved. `BULLISH_CORRECTION_TURN_MEMBERSHIP`, `BULLISH_INTERNAL_COLOR_ALTERNATION_MEMBERSHIP`, `BULLISH_DOJI_INSIDE_ACTIVE_CORRECTION`, `BULLISH_CORRECTION_NEW_HIGH_RESET`, `BULLISH_CANDIDATE_RESET_ON_NEW_HIGH`, `BULLISH_PRIOR_HL_BODY_CLOSE_INVALIDATION` and `BULLISH_PRIOR_HL_WICK_ONLY_INVALIDATION` are `CONFIRMED`; `BULLISH_CORRECTION_WINDOW`, `CANDIDATE_HL_SELECTION`, `TURN_MEMBERSHIP_SEGMENTATION` and overall structural candidate detection remain `PARTIALLY_DEFINED`.

- Direct human review of Video 2 approximately `01:40–01:55` and `06:45–06:55` confirms exact-doji behavior at correction boundaries. A candle with `Close == Open` has neutral body direction and does not start a bullish-structure or bearish-structure correction; evaluation waits for a later bearish-body or bullish-body candle respectively. If that doji prints a new `High` above the current origin ceiling or a new `Low` below the current origin floor, the price extreme still updates under the confirmed precedence rules, but directional correction does not start. No future observation may relabel the doji-time decision, and no numeric small-body tolerance is inferred. `EXACT_DOJI_CORRECTION_START = CONFIRMED_NO_START`, `DOJI_DIRECTIONAL_ROLE = CONFIRMED_NEUTRAL`, `DOJI_WITH_NEW_HIGH_PRECEDENCE = CONFIRMED`, and `DOJI_WITH_NEW_LOW_PRECEDENCE = CONFIRMED`; `NEAR_DOJI_THRESHOLD = UNRESOLVED`. Both correction windows and overall structural candidate detection remain `PARTIALLY_DEFINED`.

- Direct human review of Video 2 approximately `01:40–01:55`, `03:15–04:00` and `11:10–11:35` confirms the bullish-structure inverse precedence case. When the first bearish candle also prints a higher `High`, that new extreme first updates the correction-origin ceiling; only the candle's bearish close starts the downward correction from the updated ceiling. The ceiling is a price extreme, not an automatically validated structural HH, and wick-only `High > prior structural HH` does not bypass the causal closed-body validation rule. No finer intrabar chronology is inferred. `BEARISH_CANDLE_WITH_NEW_HIGH_PRECEDENCE`, `CORRECTION_ORIGIN_CEILING_UPDATE`, `CORRECTION_START_AFTER_UPDATED_CEILING` and `CORRECTION_ORIGIN_EXTREME_VS_STRUCTURAL_HH` are `CONFIRMED`; `BULLISH_CORRECTION_START`, `BULLISH_CORRECTION_WINDOW` and overall structural candidate detection remain `PARTIALLY_DEFINED` because doji/gap, exact inclusivity, turn membership/segmentation and bootstrap remain open.

- Direct human review of Video 2 approximately `07:05–07:25` and Video 3 approximately `07:40–07:46` confirms precedence when the first bullish candle after a bearish impulse also prints a lower `Low`: that new extreme first updates the correction-origin floor, and the candle's bullish close then starts the correction from the updated floor. The floor update is not a validated structural LL; formal LL validation still requires a closed body beyond the prior structural LL. No finer intrabar chronology is inferred. `BULLISH_CANDLE_WITH_NEW_LOW_PRECEDENCE`, `CORRECTION_ORIGIN_FLOOR_UPDATE`, `CORRECTION_START_AFTER_UPDATED_FLOOR` and `CORRECTION_ORIGIN_EXTREME_VS_STRUCTURAL_LL` are `CONFIRMED`; `BEARISH_CORRECTION_START` remains `CONFIRMED`, while `BEARISH_CORRECTION_WINDOW` and overall structural candidate detection remain `PARTIALLY_DEFINED` because doji/gap, exact inclusivity and segmentation are still open.

- Direct human review of Video 2 approximately `01:40–01:55` and `06:40–07:10` confirms the bearish correction start: after a prior LL and cessation of new lows, the first bullish candle that begins upward displacement starts the correction. The already-confirmed causal end remains a formally closed `Close < prior LL`, which validates the new LL and preceding highest candidate LH only from that `AsOfUtc`. `BEARISH_CORRECTION_START` and `BEARISH_CORRECTION_END` are `CONFIRMED`; `BEARISH_CORRECTION_WINDOW` remains `PARTIALLY_DEFINED` because doji/`Close == Open`, gaps, simultaneous bullish-body/new-low behavior, exact LL-candle inclusivity and turn segmentation remain unresolved. No precedence rule is inferred.

- Direct human review of Video 2 approximately `11:10–11:25` and Video 3 approximately `07:25–07:46`, with the previously audited single-expansive-candle context, confirms that the single-candle structural HL or LH origin coordinate is that candle's `Open`. The same candle remains excluded from correction membership/scanning: its `Open` supplies retrospective origin evidence and its causal `Close` validates the structure from that `AsOfUtc`, without an inferred intrabar path. This case-specific rule does not replace multi-candle selected-turn body-edge rules. Buy protection remains anchored at the relevant HL `Low`/lowest wick and sell protection at the relevant LH `High`/highest wick; the actual executable offset below/above those anchors remains unresolved. `SINGLE_CANDLE_ORIGIN_PRICE_COORDINATE`, `SINGLE_CANDLE_HL_STRUCTURAL_PRICE`, `SINGLE_CANDLE_LH_STRUCTURAL_PRICE`, `BUY_HL_PROTECTION_ANCHOR`, `SELL_LH_PROTECTION_ANCHOR` and `STRUCTURE_VS_PROTECTION_SEPARATION` are `CONFIRMED`; `ACTUAL_SL_OFFSET_FROM_ANCHOR` is `UNRESOLVED`; overall structural candidate detection remains `PARTIALLY_DEFINED`.

- Direct human review of Video 2 approximately `03:20–04:10` and `05:55–06:15`, plus Video 3 approximately `07:40–07:46` and `09:00–09:15`, confirms that the causal `Close > prior HH` or `Close < prior LL` candle is excluded from correction membership and retrospective candidate scanning. It belongs to the impulsive expansion; its close confirms the new HH/LL, ends the correction, validates the preceding candidate and establishes the causal `AsOfUtc`. A single expansive candle may contain impulse-origin evidence and the confirming close without becoming an ordinary correction candle. The exact origin-price coordinate in that special case and any intrabar chronology remain unresolved. `CONFIRMING_CANDLE_CORRECTION_MEMBERSHIP` and `CONFIRMING_CANDLE_SCAN_INCLUSIVITY` are `CONFIRMED_EXCLUDED`; `CONFIRMING_CANDLE_VALIDATION_ROLE` and `SINGLE_CANDLE_BREAK_ORIGIN_CASE` are `CONFIRMED`; `IMPULSE_LEG_MEMBERSHIP` remains `PARTIALLY_DEFINED`; `SINGLE_CANDLE_ORIGIN_PRICE_COORDINATE` is `UNRESOLVED`; overall structural candidate detection remains `PARTIALLY_DEFINED`.

- Direct human review of Video 2 approximately `11:10–11:35` and Video 3 approximately `07:25–07:35` confirms the structural coordinate for an already identified LH turn. The structural line uses the highest candle-body edge, `max(Open, Close)`, across candles belonging to that selected turn; candle color is non-normative and a higher wick does not redefine the coordinate. The separate sell Stop Loss anchor remains above the relevant LH's highest wick/tail. Structural identity, body-based structural price and wick-based risk anchor therefore remain distinct. `CANDIDATE_LH_STRUCTURAL_PRICE`, `BEARISH_STRUCTURAL_BODY_ANCHOR`, `SELL_LH_SL_ANCHOR` and `STRUCTURAL_IDENTITY_VS_PRICE_SEPARATION` are `CONFIRMED`; bearish correction boundaries and overall structural candidate detection remain partial.

- Direct human review of Video 3 approximately `04:25–04:35` and `07:15–07:45`, together with the prior causal LH evidence, confirms complex candidate-LH identity and highest-high replacement. Within one recognized bearish correction, a later higher candidate high replaces an earlier lower candidate and superseded intermediate highs do not survive. The preceding highest candidate becomes validated LH only from the causal formally closed `Close < prior LL` `AsOfUtc`. This does not define bearish correction edges, a structural LH price/body anchor or a target/comparison coordinate. The sell risk example separately confirms Stop Loss above the highest wick/tail of the relevant LH; that SL anchor does not redefine the structural coordinate used for targets, reconstruction or comparisons. `COMPLEX_CANDIDATE_LH_SELECTION` and `HIGHEST_HIGH_REPLACEMENT` are now `CONFIRMED`; `BEARISH_CORRECTION_WINDOW` remains `PARTIALLY_DEFINED`, `CANDIDATE_LH_STRUCTURAL_PRICE` remains `UNRESOLVED`, `BEARISH_STRUCTURAL_BODY_ANCHOR` remains `CANDIDATE`, and overall structural candidate detection remains `PARTIALLY_DEFINED`.

- Direct human review of Video 2 approximately `05:40–06:15`, `06:40` and `13:20`, plus Video 3 approximately `04:30`, explicitly confirmed bearish LH causal validation and correction origin. The retracement starts from the prior LL area; its candidate high remains provisional until a formally closed candle has `Close < prior LL`, which confirms the new LL and validates the preceding LH only from that causal `AsOfUtc`. Wick-only and open-candle penetration are insufficient. The mentor's verbal same-logic statement supports only `CANDIDATE` hypotheses for complex candidate-LH selection, highest-high replacement, a mirrored bearish correction window, candidate-LH price and bearish body anchor because no reviewed complex bearish correction demonstrates them. Overall structural candidate detection remains `PARTIALLY_DEFINED` and those hypotheses are not approved for deterministic runtime.

- Direct human review of Video 2 approximately `01:40–01:55`, `03:15–04:50` and `13:15–13:40` partially defined the bullish correction window for candidate HL. The prior HH is the upper boundary; after HH production stops, the first bearish candle starts the correction, which remains active through deeper lows and internal zigzags until a formally closed candle has `Close > prior HH`. The deepest low/base becomes validated only from that causal close `AsOfUtc`; a deeper low replaces a shallower candidate. The selected-turn lowest body `Open`/`Close` determines the candidate price, including when equal coordinates produce the same price, but does not choose candle identity. Doji/gap handling, exact candle inclusivity, turn membership and candidate-LH selection remain unresolved; structural candidate detection remains `PARTIALLY_DEFINED`.

- Direct human review of Video 2 approximately `13:15–13:40` and Video 3 approximately `07:40–07:46` partially defined candidate-HL selection. Within a human-delimited corrective phase, preliminary/intermediate lows are discarded; the candidate HL is the deepest low and the base of the impulse that later produces the causal body-close break above the prior HH. One- or two-candle pauses inside that impulse do not create another HL. Within the selected turn, its structural reference is the lowest body `Open`/`Close`, not an isolated wick. Correction/turn boundaries, candle membership, equal-coordinate tie-breaking and candidate-LH selection remain unresolved, so overall structural candidate detection remains `PARTIALLY_DEFINED`.

- Direct human review of Video 2 approximately `03:05–04:10`, `06:15–06:45` and `08:35–09:15` confirmed the causal 1H/4H structural swing-validation rule. A candidate low becomes a validated HL only after its impulse produces a formally closed candle body above the prior HH, and a candidate high becomes a validated LH only after the inverse close below the prior LL. New HH/LL likewise require a formally closed body beyond the prior structural extreme; wick-only penetration and still-open candles are insufficient. Replay may recognize the earlier candidate as validated only from the confirming close `AsOfUtc`. Initial candidate-turn detection, pivot geometry, exact body-zone coordinate, mitigation semantics and candidate ranking remain unresolved, so structural fallback remains `PARTIALLY_DEFINED`.

- Direct human review of Video 3 approximately `03:10–03:25` and `05:55–06:40`, Video 1 approximately `10:10–10:35`, and Video 2 approximately `08:35–09:20` and `11:10–11:45` partially defined structural target fallback. Sell candidates include relevant unmitigated support at a validated 1H/4H HL or LL and Previous Day Low; buy candidates include relevant unmitigated resistance at a validated 1H/4H LH or HH and Previous Day High. In reviewed paths with distinct relevant 1H and 4H levels, 1H acts as the first objective or management level and the farther, structurally stronger 4H level as final or extended. The mentor marks the structural zone from candle bodies rather than an isolated long wick. This does not define a universal hierarchy, the structural detector, exact mitigation semantics, body coordinate, decimal reduction, PDH/PDL ranking or tie handling; structural fallback remains `PARTIALLY_DEFINED` and non-automatable.

- Direct human review of Video 3 approximately `07:50–09:15`, with the alignment reference around `04:25`, resolved `NQ-Q-SW-006`. After 08:30, the first valid liquidity take is the observed Step 3 rather than target consumption before Step 3: taking a relevant Low can orient a possible buy progression and taking a relevant High can orient a possible sell progression, subject to the existing Step-1 4H permitted direction. A non-aligned orientation does not enable Step 4, does not revive the originally imagined scenario and must not be chased. Step 3 retains its audited exceed/break-below geometry; equality-only touch was not established. This is distinct from exact selected-target touch cancelling an already-active pre-entry setup and does not expand the structural 1H/4H fallback.

- Direct human source reconciliation of Video 3 approximately `03:10–03:25` and `08:50–10:50`, plus Video 1 approximately `10:10–10:30`, resolved two target-selection cases. Equal relevant Asia/London prices represent one unique target, not two sequential targets; the reviewed equal case treats the shared level as final Take Profit without an artificial intermediate Break-Even transition. If a relevant session target was consumed before the operational setup context or no longer remains the next future-valid target, the confirmed conceptual fallback is the next relevant structural 1H/4H point from Step 2. The structural detector, 1H-versus-4H priority and ranking/tie handling remain unresolved. At that audit point, the edge after 08:30 but before supposed Step-3 activation remained unresolved; the later `07:50–09:15` review recorded above supersedes that limitation. Touching an already-selected target after setup activation still cancels incomplete pre-entry progression under the existing no-chase policy; this is distinct from pre-operational fallback selection.

- Direct human review of Video 3 approximately `13:10–13:45` confirmed that a target is an absolute Asia/London price level and target touch is a timeframe-independent quote-level event. The mentor's post-entry 1M chart is a visual management surface, not the normative target timeframe. Current canonical replay exposes closed OHLC `CandleSeries`; their ranges can prove touch occurrence within an interval but cannot establish intrabar order when target touch and entry completion share the same minimum-granularity candle. This is a market-data resolution limitation, while target edge cases and final TP behavior remain separate source questions.
- Direct human review of Video 3 approximately `09:40–10:55` and `13:25–13:40` narrowed intended opposite-side targets to the Step-2 Asia/London session extrema: London/Asia Low for sells and London/Asia High for buys. When distinct, the immediate target is the first relevant level encountered in the expected price path. Exact contact with the horizontal wick extreme is sufficient without body close or tolerance. A pre-entry touch cancels the active setup; after entry, the same touch geometry supports the documented Break-Even trigger, while one reviewed case supports complete exit at its selected TP. Same-observation ordering, candidate availability/already-crossed cases and whether the first BE target always equals final TP remained unresolved at that audit point; the later `13:10–13:45` review resolved the timeframe question separately.
- Initial repository documentation.
- Based on the complete 58:24 mentorship analysis.
- Confirmed sequence separated from subjective and unresolved criteria.
- An earlier manual source-video interpretation documented preparation at 08:00 and trading from 08:30 through 11:30 in `America/Bogota`. The 11:30 cutoff is retained here only as historical traceability and is superseded by the direct human source re-review below.
- Direct human source re-review corrected the operational trading/setup cutoff from 11:30 to 11:00 `America/Bogota`. The canonical entry-acquisition interval is now `[08:30, 11:00)`: at 11:00 the mentor considers the market to be in "zona muerta," unfinished pre-entry progression expires, no dead-zone signal may revive it, and the strategy returns the next operational day. No forced close of a conceptual trade entered before 11:00 was established. Approximate supporting evidence in Video 3: `04:15–04:30`; no title or URL is inferred.
- The same human re-review clarified post-liquidity-take lifecycle: the take activates waiting for Step 4; continued manipulation-direction movement and wick-only breaks do not confirm Step 4 or alone cancel the setup; a newer extreme may update the active 5M structural reference before confirmation without deleting history; and consumption of the intended target before formal entry cancels the setup under the "do not chase price" principle. Exact target and structural-reference geometry remain unresolved.
- Direct human re-review of Video 3 approximately `06:20–06:40` and `10:30–10:55` clarified multiple liquidity-take lifecycle: same-side subsequent takes keep the same single setup pending at Step 3 while updating the active 5M reference; opposite-side target consumption cancels the prior setup; and the newly taken opposite liquidity may activate Step 3 of a new setup only when aligned with the existing 4H context. No automatic direction reversal, concurrent same-side setup or retroactive revival is supported. Exact target-touch, structural-reference and IFVG-specific interaction geometry remain unresolved.
- Manual source-video re-verification confirmed the Asia interval `[D-1 17:00, D 02:00)` and London interval `[D 02:00, D 07:00)` in `America/Bogota`, with extrema calculated from completed 1H candles by local `OpenTime`.
- Manual source-video re-verification clarified 4H HH/HL/LL/LH structure and Breakout/Wickfill/Fakeout semantics: structural breaks require body closes, HL/LH confirmation is retrospective without future leakage, and the three current-context states use `OR`. Approximate evidence: Breakout `02:20–02:40`, Wickfill `04:23–04:32` and `06:55–07:25`, Fakeout `10:40` and `10:55–11:25`; pivot/retracement selection, zone geometry, Wickfill completion and classification precedence remain unresolved.
- Human re-review clarified retrospective 4H structural-point confirmation, selection of the relevant retracement turning extreme after the confirming break, rejection of minor fluctuations, body-based structural marking and the 08:00 closed-4H filter. Approximate evidence: `03:05–04:55`, `06:20–07:35`, `08:35–09:20` and `11:10–11:45`; prior-level bootstrap, retracement boundaries and exact numeric body coordinates remain unresolved.
- Human review of Video 3 clarified the end-to-end Nasdaq workflow as a strict chronological gate from 4H context through separate liquidity marking/take, 5M Structural Change `OR` IFVG, mandatory 5M FVG confirmation, 1M pullback/realignment and entry eligibility, followed by structural-wick Stop Loss, directional liquidity Take Profit and first-important-liquidity Break-Even concepts. Asia/London levels remain separate liquidity references and do not seed 4H structure; exact algorithms and runtime status mapping remain unresolved.
- The same re-verification resolved the prior Stop Loss interpretation in favor of the structural 5M HL for buys and structural 5M LH for sells; deterministic swing geometry remains unresolved.
- Take Profit direction was confirmed as important highs for buys and important lows for sells; the deterministic meaning of important remains unresolved.
- The six-stage strategy sequence was re-verified directly against the source video.
- News and reentry policies remain unresolved.
- Not approved for autonomous execution.
