# MoneyWay Nasdaq strategy specification

## 1. Metadata

| Field | Value |
|---|---|
| Strategy | MoneyWay Nasdaq |
| Specification version | `nasdaq-0.1.0-draft` |
| Status | draft |
| Source material | Complete audited analysis of the 58:24 mentorship video, including direct manual source-video re-verification |
| Real-money trading | Prohibited |
| Autonomous execution | Not approved |

## 2. Purpose and scope

Esta especificación conserva exclusivamente la evidencia auditada para MoneyWay Nasdaq. Describe análisis, backtesting y paper trading supervisado; no contiene reglas de MoneyWay Forex ni autoriza ejecución autónoma.

## 3. Evidence policy

Cada regla usa `confirmed`, `candidate`, `context_specific`, `visual_only`, `human_validation_required`, `unresolved` o `rejected_ai_inference`. Una regla `confirmed` puede ser no automatizable si depende de juicio subjetivo. Los valores no definidos permanecen `null`, `unresolved` o `human_validation_required`.

Los resultados permitidos son `passed`, `failed`, `waiting`, `not_applicable`, `human_validation_required` y `data_unavailable`. El veredicto general es `ready`, `wait`, `no_trade`, `human_validation_required` o `data_unavailable`.

## 4. Source coverage

La documentación representa la evidencia actualmente auditada del video de 58:24, consolidada desde cinco intervalos y re-verificaciones humanas directas de los videos fuente. Estas re-verificaciones confirmaron la secuencia de seis etapas, preparación a las 08:00 y workflow operativo `[08:30, 11:00)` en hora Colombia, las sesiones Asia/London y sus extremos 1H, la guía de Stop Loss 5M HL/LH, los targets en important highs/lows y los conceptos estructurales 4H descritos en la sección 9. La evidencia humana directa más reciente corrige 11:30 como una interpretación anterior incorrecta: a las 11:00 comienza la "zona muerta" y no se continúa buscando o completando una entrada. Video 3 aproximadamente `04:15–04:30` también evidencia días sin liquidity take o sin confirmación estructural/inversa, para los cuales no hay trade y se vuelve al siguiente día operativo. Una revisión humana del segundo video aclaró la confirmación retrospectiva y selección visual de puntos estructurales aproximadamente en `03:05–04:55` y `06:20–07:35`, el uso de cuerpos aproximadamente en `11:10–11:45` y el filtro higher-timeframe aproximadamente en `08:35–09:20`. La revisión humana posterior, especialmente de Video 3, confirmó el workflow end-to-end como una secuencia de gates obligatorios y aclaró la separación entre 4H context, session liquidity, 5M trigger, 5M FVG confirmation, 1M realignment y management. A further direct human review of Video 3 at approximately `06:20–06:40` and `10:30–10:55` clarified the direction-dependent lifecycle of multiple liquidity takes: same-side extension updates the active reference within the same pending setup, whereas opposite-side target consumption cancels it and can only support a new setup when aligned with Step-1 4H context. Direct human review of Video 3 at approximately `09:40–10:55` and `13:25–13:40` narrowed the target role to the directionally relevant Asia/London session extrema and confirmed exact touch semantics for cancellation and management. Review at approximately `13:10–13:45` confirmed that the target is an absolute price level and its touch is a timeframe-independent quote-level event; the 1M chart is the mentor's visual post-entry management surface, not part of the target definition. Los timestamps suministrados son aproximados; no se inventan título, URL ni líneas de transcript. Salvo esos intervalos aproximados y `50:15` para riesgo máximo por operación, los timestamps de reglas individuales siguen siendo `null` cuando no fueron proporcionados.

The latest direct review of Video 3 approximately `03:10–03:25` and `05:55–06:40`, Video 1 approximately `10:10–10:35`, and Video 2 approximately `08:35–09:20` and `11:10–11:45` partially defines that structural fallback. For sells, source-supported candidates are relevant unmitigated support represented by a validated 1H/4H HL or LL, plus Previous Day Low. For buys, they are relevant unmitigated resistance represented by a validated 1H/4H LH or HH, plus Previous Day High. In reviewed paths containing distinct relevant 1H and 4H levels, 1H acts as the first objective or management level and the farther 4H level as the final or extended objective; 4H is treated as structurally stronger. This observed hierarchy is not generalized beyond the reviewed evidence. Structural zones are marked from candle bodies rather than an isolated long wick. The selected-turn candidate-HL and LH body coordinates are now defined as documented below, but initial candidate-turn detection and coordinates for other structural-point classes remain unresolved. No title, URL or transcript wording is inferred from these approximate references.

Direct human review of Video 2 approximately `03:05–04:10`, `06:15–06:45` and `08:35–09:15` confirms the causal validation rule for structural swings on the relevant 1H or 4H timeframe. A candidate HL/LH becomes validated only after the impulse originating from it produces a body close beyond the preceding HH/LL, and a new HH/LL requires that formally closed candle rather than wick-only penetration. Candidate detection and exact body-zone price remain separate unresolved boundaries.

Direct human review of Video 2 approximately `01:40–01:55`, `03:15–04:50`, `06:30`, `11:10–11:25`, `12:20` and `13:15–13:40`, plus Video 3 approximately `07:25–07:46` and `09:00`, partially defines the bullish correction window used for candidate-HL selection and confirms exact equal-price structural identity. From the prior HH peak, once the bullish impulse stops producing HHs, the first bearish candle starts the correction and the prior HH remains its upper structural validation reference. Equality and wick-only penetration of that reference remain non-confirming and, absent a separate terminal event, included in the active turn. A strict crossing of the distinct correction-origin ceiling resets that turn when the same candle does not also confirm the candidate. The correction remains active through internal zigzags and deeper lows until a formally closed candle has `Close > prior HH structural level`; only at that causal close does the deepest candidate become a validated HL. The candidate is the deepest low/base of the correction and of the impulse that produces the break; preliminary/intermediate lows are replaced by a deeper low, and one- or two-candle pauses inside that impulse do not create another HL. For the supplied turn, the structural reference uses the lowest `Open` or `Close` among its candle bodies, not an isolated wick. Multiple member candles at that exact resulting body price support one structural price level; no candle ownership or first/last/timestamp tie-break is required. Near-doji thresholds and any tolerance for merging merely near-equal prices remain unresolved.

Direct human review of Video 2 approximately `09:25–10:15` and `12:45–13:40`, especially `13:00–13:10`, confirms general in-range membership for an already-active bullish correction. Bearish, bullish and exact-doji candles, internal oscillations and consolidations remain in the same correction turn while no terminal/reset event occurs; body-color alternation does not fragment the turn or create parallel HL candidates. A strict `High > current correction-origin ceiling` resets the active correction and discards its candidate HL when that candle does not also confirm the candidate with `Close > prior structural HH`. The same non-confirming reset observation is then classified by body direction: when `Close < Open`, the candle is excluded from the old turn and included as the first member of the new bullish-correction turn; when `Close == Open` or `Close > Open`, no new correction starts until a later qualifying bearish-body candle. The separately confirmed confirmation-plus-extension collision validates the old candidate instead of discarding it; an exact corrective body starts the next turn, while any other body leaves `AwaitingCorrectionStart`. These same-observation rules use closed OHLC only and do not reconstruct an intrabar path. An updated ceiling by itself is not a validated structural HH. Independently, a formally closed `Close < prior validated HL structural reference` invalidates the bullish structure and terminates candidate-HL pursuit; equality and wick-only penetration do not. The causal invalidation candle is excluded from the destroyed correction turn and its candidate-HL geometry, cannot change that candidate's `StructuralPrice` or `ProtectionAnchor`, and supplies evidence only for a new bearish impulse rather than an automatically validated structural LL. Membership is fixed at the candle's closed boundary and cannot be reinterpreted by later candles.

Direct human review of Video 2 approximately `08:50–10:15` and Video 3 approximately `08:50–09:15`, with additional context around Video 3 `07:25`, resolves the collision in which the same formally closed candle has both `High > current correction-origin ceiling` and `Close < prior validated HL`. Both observations remain true, but for the structural transition the prior-HL invalidation dominates the new-High reset. At that causal `AsOfUtc`, the bullish structure and its candidate HL are destroyed; no new bullish correction continues from the collision candle, which is excluded from the old turn and candidate geometry and belongs to the new bearish impulse context. Its `High` remains a relevant upper price extreme and bearish-origin reference, but is not automatically a validated LH, validated structural High or executable Stop Loss. It cannot retrospectively change the old candidate's body-based `StructuralPrice` or wick-based `ProtectionAnchor`; the new bearish context may retain the collision candle's upper wick as protection-reference evidence, while the executable offset and price remain unresolved. This rule compares the closed OHLC facts with zero tolerance and infers no intrabar sequence. Earlier replay snapshots remain unchanged; the bearish result is knowable only from the collision candle's close onward. This structural collision semantics is not automatically mapped to `NQ-LIQ-003`, `NQ-M5-001`, IFVG/FVG confirmation or entry gating, and establishes no same-frame workflow propagation.

Direct human review of Video 2 approximately `01:40–01:55`, `05:40–06:15`, `06:40–07:10`, `11:10–11:35` and `13:20`, plus Video 3 approximately `04:25–04:35`, `04:30`, `07:15–07:45` and `07:25–07:35`, explicitly confirms the bearish correction start, causal-validation path, complex candidate-LH identity and structural LH coordinate. After a prior LL, once the bearish impulse stops producing new lows, the first bullish candle that begins the upward displacement starts the bearish correction. Its candidate high remains unvalidated while that LL lacks a confirmed break. Within one recognized bearish correction, a later higher candidate high replaces an earlier lower one; superseded intermediate highs do not survive as the LH candidate. Only a formally closed candle with `Close < prior LL structural level` ends the correction, confirms the new LL and validates that preceding highest candidate high as LH from that causal `AsOfUtc`; wick-only penetration and an open candle are insufficient. For an already selected LH turn, the structural coordinate is the highest candle-body edge in that turn: `max(Open, Close)` across its member candles. Candle color does not govern the coordinate, and a higher wick does not redefine it. Separately, the sell risk example places Stop Loss above the highest wick/tail of the relevant LH. That SL/invalidation anchor remains distinct from the body-based structural coordinate used for target selection, reconstruction or comparisons.

Direct human review of Video 2 approximately `07:05–07:25` and Video 3 approximately `07:40–07:46` resolves the bearish-start case in which the first bullish candle also prints a lower `Low`. The new lower price extreme takes precedence over treating the candle as having already started the correction: its `Low` first replaces the prior correction-origin floor, and its bullish close then starts the upward correction from that updated floor. This price-floor update does not confirm a new structural LL. Structural LL validation remains governed by a formally closed body below the prior structural LL. The evidence establishes only the new lower extreme and the eventual bullish close, not a finer tick sequence or synthetic intrabar path.

New direct human review of Video 2 approximately `06:40` and `12:15–12:35`, Video 3 approximately `04:30`, and the reviewed bearish collision confirms that active-correction terminal mechanics are an exact causal mirror between bullish and bearish structure. During an active bearish correction, bullish, bearish and exact-doji candles, internal oscillation and consolidation remain in one turn while neither reset nor invalidation occurs. A strict `Low < current correction-origin floor` ordinarily resets the correction, discards its candidate LH and updates the floor to that lower price extreme without validating a structural LL; simultaneous strict `Close < prior LL` instead validates that candidate and establishes the new LL-side reference. For both ordinary non-confirming reset and confirmed candidate-validation plus extension, exact `Close > Open` excludes the candle from the old turn and includes it as the first member of the next bearish-correction turn; `Close == Open` or `Close < Open` starts no new correction. Independently, a formally closed `Close > prior validated LH` invalidates bearish structure with zero tolerance; equality and wick-only `High > prior LH` with `Close <= prior LH` do not. The invalidation candle is excluded from the destroyed turn and candidate-LH geometry, cannot alter the old `StructuralPrice` or `ProtectionAnchor`, and belongs to the new bullish impulse context without automatically validating an HL or HH.

When one formally closed candle has both `Low < current correction-origin floor` and `Close > prior validated LH`, both observations remain true but prior-LH invalidation dominates the state transition. The candidate LH is destroyed, no new bearish correction starts, and the candle belongs to the new bullish impulse context. Its `Low` remains a relevant lower price extreme, bullish-origin reference and wick-based protection evidence, but is not automatically a validated HL, Structural Low or executable Stop Loss. No intrabar order or retroactive history mutation is inferred; the result exists only from that causal close onward.

Direct human review of Video 2 approximately `02:20–02:30`, supported by the body-close evidence around `06:25–06:55`, confirms that an opening/quotation gap where `previous.Close != current.Open` has no special structural role. Gap existence or size does not start or reset a correction, invalidate structure, fragment a turn, create a structural point or validate one. Correction start remains governed by exact body direction; reset remains governed by strict `High`/`Low` origin-extreme rules; invalidation remains governed by the formally closed `Close` beyond the prior validated HL/LH. No gap-size threshold applies. The missing interval between the prior close and current open is not interpolated, traversed synthetically or filled with invented candles. This opening discontinuity is distinct from the strategy's multi-candle FVG/IFVG concepts and does not satisfy Step 4, Step 5 or Step 6.

In the reviewed collision example only, the mentor interprets the lower-wick/new-Low event as a liquidity take and the body close above the prior LH as structural change, presenting that candle as the beginning of the new bullish impulse. This evidence is `context_specific`: it does not establish that every structural collision satisfies `NQ-LIQ-003` or `NQ-M5-001`, does not bypass 4H alignment, selected-liquidity, timeframe or workflow prerequisites, and does not authorize same-frame prerequisite propagation. The canonical progression fold currently determines eligibility from rules established before the current replay frame, so a future human-validated mapping of both gates to one causal frame would expose an unresolved same-frame gating discrepancy. This documentation does not change the prerequisite graph or choose a resolution.

Direct human review of Video 2 approximately `03:20–04:10` and `05:55–06:15`, plus Video 3 approximately `07:40–07:46` and `09:00–09:15`, confirms the role of the causal break candle in both directions. The formally closed candle with `Close > prior HH` or `Close < prior LL` is excluded from the correction-candle set scanned retrospectively for the candidate HL/LH. Relative to the old turn, it belongs to the expansion/impulsive leg; its close confirms the new HH/LL, closes that correction, validates the preceding candidate and establishes the causal `AsOfUtc`. In a confirmation-plus-origin-extension event, an exact corrective body can also start the next turn without entering the old geometry. In the demonstrated single-expansive-candle case, that same candle may contain evidence of the impulse origin at the selected turn while remaining excluded as an ordinary correction member.

Direct human review of Video 2 approximately `11:10–11:25` and Video 3 approximately `07:25–07:46` confirms the exact structural-origin coordinate for that single-candle case. For both a bullish HL origin and a bearish LH origin, the structural coordinate is the confirming candle's `Open`: the bullish candle starts from the base of its body and the bearish candle starts from the top of its body. Its `Open` supplies retrospective origin evidence only once the causal close is observable; its `Close` supplies the validation event. No synthetic intrabar chronology is required or permitted. This single-candle rule does not replace the existing multi-candle selected-turn body-edge rules. Protection remains separate: the buy HL anchor is the candle `Low`/lowest wick extreme and the sell LH anchor is the candle `High`/highest wick extreme, while the executable Stop Loss remains below/above that anchor by an unresolved offset.

## 5. Supported operating modes

| Mode | Readiness |
|---|---|
| Assisted analysis | Sufficient |
| Manual backtesting | Sufficient |
| Semi-automatic backtesting | Partially sufficient; human validation required |
| Supervised paper trading | Sufficient only with human validations |
| Fully automatic backtesting | Not sufficient |
| Supervised demo execution | Not enabled until critical timestamps and blockers are resolved |
| Autonomous execution | Not sufficient and prohibited |
| Real-money trading | Prohibited |

## 6. High-level workflow

1. At 08:00 `America/Bogota`, review the newly closed 4H candle against the existing valid HH/HL or LL/LH structural state, evolve that state only on a qualifying structural event, classify Breakout `OR` Wickfill `OR` Fakeout and determine the direction permitted for the subsequent setup. A human-selected initial anchor is needed only when bootstrapping otherwise unlabeled history, not on each new day.
2. Separately mark Asia High/Low, London High/Low and relevant 1H/4H structural liquidity references. Session extrema do not initialize 4H market structure.
3. From 08:30, wait for price to exceed a directionally relevant marked High or go below a directionally relevant marked Low. This liquidity take is mandatory.
4. Only after Step 3, move to 5M and require Structural Change/ChoCH/MSS `OR` IFVG, whichever occurs first.
5. After the Step-4 trigger, separately require the displacement in the intended direction to leave the mandatory 5M FVG confirmation. A Step-4 IFVG does not automatically satisfy Step 5.
6. Only after Step 5, move to 1M, require a counter-direction pullback toward/into the relevant 5M FVG and then require structural realignment with the intended direction before entry eligibility.

### 6.0 Preparation completion (`NQ-TIME-003`)

For the same trading day and morning session `D`, preparation consists of both Step 1 and Step 2. Step 1 follows the formally closed relevant 4H candle: review that candle, establish the larger structural context and permitted bullish/bearish direction, and classify the reviewed context as Breakout `OR` Wickfill `OR` Fakeout. Its initial-anchor and other human-validation limits remain. Step 2 records Asia High/Low, London High/Low and applicable relevant 1H/4H structural liquidity references under the existing rules; deterministic session extrema do not make `NQ-LIQ-002` fully automatic. The strategy-semantic completion event is **Step 1 complete AND Step 2 complete for `D`**, with evidence that both activities actually occurred. `TIME_REACHED != PREPARATION_COMPLETED`: neither an 08:00 clock reading nor available candles prove completion.

| Preparation classification | Status | Meaning |
|---|---|---|
| `NASDAQ_PREPARATION_CONTENT` | `confirmed` | Both Step 1 and Step 2, including their existing human-dependent parts. |
| `NASDAQ_PREPARATION_COMPLETION` | `confirmed` | Both steps actually completed for the same `D`, not merely observable market data. |
| `NASDAQ_PREPARATION_WINDOW` | `confirmed` | `[08:00:00, 08:30:00)` in `America/Bogota`; 08:02, 08:10, 08:28, 08:29 and 08:29:59.xxx are temporally eligible. |
| `NASDAQ_PREPARATION_DEADLINE` | `confirmed` | Completion must occur strictly before 08:30:00; 08:28 is an example, not a cutoff. |
| `NASDAQ_PREPARATION_LATE_SESSION_OUTCOME` | `confirmed_not_allowed` | If incomplete at 08:30:00, Step 3 is unauthorized and `D` is a no-trade session; later completion cannot revive it. |
| `NASDAQ_PREPARATION_SESSION_IDENTITY` | `confirmed` | Completion belongs only to `D`'s morning workflow, does not carry into the afternoon or another day, and expires with that session. |
| `NASDAQ_PREPARATION_RUNTIME_INPUT_CONTRACT` | `confirmed` | [ADR 0006](../../decisions/0006-define-nasdaq-preparation-replay-input-contract.md) specifies the immutable canonical input contract; the observation and bounded `StrategyReplayContext` transport are implemented. |
| `PREPARATION_SESSION_IDENTITY` | `confirmed` | One Nasdaq `America/Bogota` trading day `D`, bound to the exact strategy/version and replay instrument. |
| `PREPARATION_COMPLETION_OBSERVATION` | `confirmed` | One positive, source-backed assertion that both mandatory steps actually completed for `D`; no negative or partial-step event is required. |
| `PREPARATION_COMPLETION_TIMESTAMP` | `confirmed` | One authoritative UTC causal timestamp for when full completion became source-observable, compared in `America/Bogota`. |
| `PREPARATION_REPLAY_OBSERVABILITY` | `confirmed` | The assertion is visible only when its causal timestamp is at or before `StrategyReplayContext.AsOfUtc`; later evidence cannot change earlier frames. |
| `PREPARATION_CROSS_SESSION_REUSE` | `confirmed_not_allowed` | An assertion for `D-1`, `D+1`, another session, strategy or instrument cannot satisfy `D`; no latest-across-sessions fallback. |
| `PREPARATION_DUPLICATE_OBSERVATION_POLICY` | `confirmed` | At most one canonical completion assertion per exact replay identity and `D`; ambiguous duplicate/conflicting input is rejected before evaluation. |
| `NASDAQ_PREPARATION_PRE_WINDOW_RUNTIME_STATUS` | `confirmed` | Before 08:00:00, the formal preparation phase has not begun; `NQ-TIME-003` is `not_applicable`, even if an out-of-window observation exists. |
| `NASDAQ_PREPARATION_ACTIVE_WAITING_STATUS` | `confirmed` | From 08:00:00 inclusive to 08:30:00 exclusive, no qualifying same-session completion visible at `AsOfUtc` yields `waiting`; the deadline remains reachable. |
| `NASDAQ_PREPARATION_COMPLETED_RUNTIME_STATUS` | `confirmed` | A same-session completion visible at `AsOfUtc` and stamped in `[08:00:00, 08:30:00)` yields `passed`, including after the deadline. |
| `NASDAQ_PREPARATION_DEADLINE_FAILURE_STATUS` | `confirmed` | At and after 08:30:00, absence of a timely completion for `D` yields `failed`; no explicit negative record is needed. |
| `NASDAQ_PREPARATION_LATE_OBSERVATION_STATUS` | `confirmed` | A completion stamped at or after 08:30:00 remains replay evidence but cannot satisfy the deadline; the result for `D` is `failed`. |
| `NASDAQ_PREPARATION_NO_RECOVERY` | `confirmed_not_allowed` | A missed deadline cannot transition from `failed` to `passed` through a later completion; only a new session has a new opportunity. |
| `NASDAQ_PREPARATION_EVALUATOR_READINESS` | `confirmed` | Session identity, bounded input, causal timestamp, exact window and all reachable rule statuses are specified; the deterministic evaluator can be implemented, but is not yet registered. |

The canonical `NQ-TIME-003` rule-status matrix uses the `America/Bogota` local projection of `StrategyReplayContext.AsOfUtc` as `t`. A completion participates only for the exact strategy/version, replay instrument and session `D`, and only when its causal timestamp is at or before `AsOfUtc`. A timestamp before 08:00:00 or at/after 08:30:00 is not a qualifying completion.

| Local `t` for `D` | Qualifying same-session completion visible by `AsOfUtc` | `NQ-TIME-003` `RuleEvaluationResult` |
|---|---|---|
| `t < 08:00:00` | Any | `not_applicable` |
| `08:00:00 <= t < 08:30:00` | No | `waiting` |
| `08:00:00 <= t < 08:30:00` | Yes, stamped in `[08:00:00, 08:30:00)` | `passed` |
| `t >= 08:30:00` | Yes, stamped in `[08:00:00, 08:30:00)` | `passed` |
| `t >= 08:30:00` | No observation, or only a pre-08:00 assertion | `failed` |
| `t >= 08:30:00` | Only an assertion stamped at/after 08:30:00 | `failed` |

At exactly 08:00:00, the relevant 4H candle has formally closed and the rule becomes applicable: without an already observable qualifying assertion it is `waiting`, not an early failure. At exactly 08:30:00 the window is closed: a timely visible assertion yields `passed`; otherwise the rule yields `failed`. An assertion stamped before 08:00:00 never qualifies: before 08:00 the rule is `not_applicable`, during the open window it remains `waiting` absent other qualifying evidence, and from 08:30 it is `failed`. An assertion stamped at 08:30:00 or later remains available for audit but cannot recover a failed `D`. An assertion for `D-1` or `D+1` never satisfies `D`; there is no latest-session fallback. A future-stamped assertion cannot alter a prior replay frame: for example, an 08:20 completion is invisible at 08:10 (`waiting`) and visible at 08:20 (`passed`). The positive-only input contract requires no negative completion record. The next preparation cycle belongs to the next trading day.

`passed` records only that timely completion was asserted, not that the human-dependent Step 1 and Step 2 analysis was correct or that Step 3 is already open. The separate `NQ-TIME-001` opens operational Step 3 at 08:30; `NQ-TIME-002` closes unfinished operational pre-entry progression at 11:00. A timely `NQ-TIME-003` remains `passed` after 11:00 even when `NQ-TIME-002` fails. The evaluator must eventually return a `ReplayRuleEvaluationDecision` with rule status and evidence, never a `StrategyVerdict`. Higher-level logic may derive `NoTrade` from a failed required rule; `no_trade` is not a rule status. `NQ-TIME-003` is required and confirmed, but its replay capability remains `NotImplemented` because no evaluator is registered. The Step-3 prerequisite graph still omits `NQ-TIME-003`, so the confirmed session-level gate is **not yet enforced** by canonical replay. Neither clock time nor candle availability creates a completion assertion.

The operational entry-acquisition workflow starts at 08:30 and ends at 11:00, always in `America/Bogota`, with interval `[08:30, 11:00)`. Times before 11:00 may remain actionable; 11:00 and later are the mentor's "zona muerta" and are not actionable for a new or unfinished setup. After 08:30, the first valid take of a relevant Low or High is the effective Step 3 of the scenario it may orient; there is no separate target-consumption interval before Step 3. Taking a Low can orient a possible buy progression and taking a High can orient a possible sell progression, but Step 1 remains mandatory and only its 4H permitted direction can authorize progression toward Step 4. Entry order mechanics remain unresolved. For buys, the Stop Loss anchor is beyond the wick of the latest relevant structural 5M HL and the primary session target candidates are London High and Asia High. For sells, the Stop Loss anchor is beyond the wick of the latest relevant structural 5M LH and the primary session target candidates are London Low and Asia Low. When two future-valid candidates differ, the immediate target is the first relevant level price encounters in the expected direction; neither session has fixed priority. Equal candidates form one shared level, not two targets, and the reviewed case treats it as final Take Profit without a distinct intermediate BE target. If a relevant session level was consumed before the operational period or is otherwise no longer a future objective before the operational scenario, Step-2 structural context supplies a conceptual fallback. For a sell, the candidates are relevant unmitigated support at a validated 1H/4H HL or LL and Previous Day Low; for a buy, they are relevant unmitigated resistance at a validated 1H/4H LH or HH and Previous Day High. In the reviewed multi-level paths, a distinct 1H level acts as the first objective or management level and the farther, structurally stronger 4H level as the final or extended objective. This is an evidence-bounded hierarchy, not a universal ranking formula. That fallback does not rescue an originally imagined scenario invalidated by an opposite-liquidity take after 08:30. Every selected target must ultimately be an absolute numeric price level whose touch is timeframe-independent and requires no body close, candle close or tolerance. Exact structural detection, mitigation geometry, structural coordinates beyond the documented selected-turn HL/LH body anchors, PDH/PDL ranking, Stop Loss offsets, same-observation ordering under coarse historical data and universal final Take Profit behavior remain unresolved or limited as described below.

The workflow is strictly sequential. A downstream signal observed in isolation is not valid for this strategy unless every mandatory prerequisite occurred in chronological order. This dependency statement does not itself assign `passed`, `failed`, `waiting` or `not_applicable`; runtime status mapping requires a separate audit.

Direct human source evidence states the governing gate principle conceptually: **“Cada vez que yo hago un paso, eso me habilita a ir al siguiente. Si ese paso no está chequeado, no puedo ir al siguiente. La estrategia no me permite saltarme los pasos.”** A later market event therefore cannot authorize skipping Step 4, Step 5 or Step 6.

### 6.1 Workflow dependency matrix

| Gate | Prerequisite | Dependency semantics |
|---|---|---|
| Step 1: 4H context | Information observable through the 08:00 closed 4H candle | Establishes context and permitted direction through human validation |
| Step 2: liquidity marking | Preparation workflow and required session/structural inputs | Produces liquidity references; does not seed 4H structure |
| Step 3: liquidity take | Steps 1 and 2 completed for `D` before 08:30, a relevant Step-2 level and the 08:30 operational start | Enables Step 4 only after the relevant High is exceeded or Low is broken below; missing preparation forbids Step 3 for `D` |
| Step 4: 5M trigger | Valid Step-3 liquidity take | Structural Change `OR` IFVG, whichever occurs first |
| Step 5: 5M FVG confirmation | Valid Step-4 trigger | Separate mandatory directional FVG gate |
| Step 6: 1M pullback and realignment | Valid Step-5 FVG | Countertrend pullback into/toward the FVG, followed by intended-direction realignment |
| Entry concept | All mandatory preceding gates | Eligibility only; no order type or execution is defined |

Rule ownership remains separated: `NQ-H4-001` owns the Step-1 4H context and permitted direction; `NQ-LIQ-002` owns human-approved relevant liquidity marking and any pre-operational structural-fallback input; `NQ-LIQ-003` owns the first valid post-08:30 take, its effective Step-3 orientation and its required alignment with Step 1; `NQ-M5-001` owns only the Step-4 Structural Change `OR` IFVG gate after an aligned Step 3. Discarding the originally imagined pre-Step-3 scenario belongs to the Step-3 interpretation. Cancelling an already-active setup after its selected target is touched remains a separate lifecycle concern.

At `StrategyReplayContext.AsOfUtc`, only prerequisites and confirming events observable at or before that timestamp may participate. A future liquidity take cannot activate an earlier Step 4, a future 5M FVG cannot activate an earlier Step 6, and a future 1M realignment cannot create past entry eligibility. Backtesting must not find a later successful pattern and retroactively assume its earlier gates were valid.

### 6.2 Pre-entry lifecycle and cutoff

- A valid liquidity take activates waiting for Step 4; it is not entry eligibility.
- When price takes another relevant level on the same side or establishes a farther extreme in the same manipulation direction before Step 4, the same single pending setup remains active at Step 3. The second take neither advances the workflow counter nor creates a concurrent setup.
- For that same-side continuation, the newer extreme replaces the current active 5M structural reference from that observation onward. In the reviewed bearish-reversal example, the newer High becomes active and the newer relevant preceding 5M Low becomes the level watched for a later bearish body-close break. Prior references remain in the audit history; they are not deleted.
- Repeated valid same-side extensions may continue updating the current active reference before Step 4. The source defines no numeric maximum.
- A wick-only break does not confirm Step 4 and does not alone cancel the setup. Before 11:00, the strategy continues waiting when no other audited cancellation occurred.
- Before the operational scenario, a session target already consumed or otherwise no longer future-valid is a target-selection input: the source falls back conceptually to the next relevant structural 1H/4H point. This is distinct from cancellation and does not define a deterministic structural-point selector. At or after 08:30, however, the first valid opposite-liquidity take is itself the effective Step 3 for the newly oriented scenario; it is not a pre-Step-3 target-consumption edge and structural fallback does not rescue the originally imagined scenario.
- A valid Low take can orient a possible buy progression and a valid High take can orient a possible sell progression. That orientation may continue toward Step 4 only when it matches the existing Step-1 4H permitted direction. If it does not match, Step 4 remains disabled, the prior imagined scenario remains discarded, and there is no countertrend trade, revival or chase.
- If price instead touches the intended opposite-side target before completed pre-entry progression, the active setup is cancelled from that observation onward because the expected move occurred without valid entry eligibility. For sells the candidates are London Low and Asia Low; for buys they are London High and Asia High. When distinct, the immediate target is the first relevant candidate encountered in the expected price path. Exact contact with the horizontal session-extreme level is sufficient, including wick contact; no body close, candle close, penetration or tolerance is required. Price must not be chased. A later 5M or 1M pattern cannot revive or retroactively validate the cancelled setup.
- Opposite-side liquidity consumption does not automatically reverse the permitted trade direction. Liquidity direction alone is not trade direction. If the newly taken opposite-side liquidity is aligned with the existing Step-1 4H permitted direction, it may activate Step 3 of a new chronological setup; otherwise it authorizes no countertrend setup. The new setup is not a revival of the cancelled one.
- The source does not define how same-side active-reference updates affect the IFVG branch specifically. Step 4 remains Structural Change `OR` IFVG, whichever occurs first, and no IFVG reset rule is inferred.
- All Steps 3–6 and entry eligibility must complete before 11:00. At 11:00, unfinished pre-entry progression expires, there is no entry for that day, and the strategy returns the next operational day. A later dead-zone signal cannot revive the setup. For example, a 10:55 liquidity take followed by an 11:03 structural change cannot complete that expired setup.
- This cutoff governs entry acquisition. It does not establish forced closure at 11:00 for a conceptual trade entered earlier; post-entry Stop Loss, Take Profit and Break-Even management remain separate.

### 6.3 Target-consumption boundary

Step-3 liquidity take, selected-target interaction and structural-break confirmation use different geometries. Step 3 retains the audited requirement that price exceed a selected relevant High or go below a selected relevant Low; equality-only contact is not confirmed. Step-1 and Step-4 structural breaks retain their audited body-close requirements. Selected-target and management interaction is instead confirmed by a price quote touching the exact absolute target level, without a close or tolerance. This `TARGET_TOUCH_EVENT` is timeframe-independent. The mentor visually monitors and manages the post-entry trade on 1M, but the chart timeframe does not define the target; a platform/broker order may react to the fixed price level without manual chart interaction.

With historical candles, a range containing the target establishes only `TARGET_WAS_TOUCHED_DURING_INTERVAL`: a downside target can be observed through the candle Low and an upside target through the candle High. This is an observational property of OHLC, not a candle-based target definition. A closed candle does not expose the exact quote timestamp or intrabar path. Therefore one minimum-granularity candle that both touches the target and supplies the final event needed to complete `NQ-M1-003` does not establish which happened first. No Open→High→Low→Close path, Open→Low→High→Close path, candle-direction ordering, interpolation, synthetic tick or later-candle reconstruction is permitted. `SAME_OBSERVATION_ORDERING_UNRESOLVED` is retained as a market-data resolution limitation rather than an unresolved target-timeframe rule. Equal Asia/London prices form one target occurrence. A session target consumed before the operational period or otherwise no longer future-valid before the operational scenario invokes the conceptual structural fallback; deterministic structural selection remains unresolved. A valid post-08:30 take instead defines Step 3 and does not invoke fallback to preserve the discarded scenario.

### 6.4 Conceptual setup lifecycle and chronology

These labels classify source-supported concepts; they are not runtime enum names:

| Conceptual state | Source-supported transition |
|---|---|
| `ACTIVE` | The first directionally valid Step-3 liquidity take creates one pending setup waiting for Step 4. |
| `REFERENCE_UPDATED` | At a later timestamp, a farther same-side take keeps that setup active at Step 3 and replaces only its current structural reference. |
| `CANCELLED` | Opposite-side target consumption before formal entry ends the old setup from that timestamp onward. |
| `NEW_SETUP` | After cancellation, the opposite take may activate a separate new Step 3 only when it agrees with the existing Step-1 4H permitted direction. |

Chronology is strict. If `T1` activates the setup and `T2` supplies a farther same-side extreme, observations before `T2` retain the old reference and only observations from `T2` onward use the updated reference. A Step-4 confirmation at `T3` must be evaluated against the reference active at `T3`; it cannot rewrite `T1` or `T2`. If cancellation occurs at `Tcancel`, later patterns cannot retroactively create an entry for the cancelled setup. Any source-permitted activation after `Tcancel` is a new progression, not revival. The 11:00 cutoff expires every unfinished pre-entry progression regardless of prior reference updates.

## 7. Buy workflow

| Step | Rule status | Required inputs | Evaluation result | Blocking behavior | Human validation requirement | Open variables |
|---:|---|---|---|---|---|---|
| 1. 4H HH/LL context | `confirmed` + `human_validation_required` | Closed 4H candles | Direction inherited from the latest confirmed break, active structural pair, scenario and permitted direction reviewed | Blocks all lower stages | Yes | Initial-anchor selection, historical scan termination, other structural-point coordinates and retracement boundaries |
| 2. Breakout/Wickfill/Fakeout | `confirmed` conceptually | 4H context and relevant level | One classification recorded | Unclassified context blocks | Yes | Exact geometries and tolerances |
| 3. Relevant liquidity | `confirmed` + `human_validation_required` | Session levels and 1H/4H context | Asia/London extrema and structural coincidences recorded separately from 4H bootstrap | Missing levels block sweep evaluation | Yes | Structural-point algorithm, coincidence tolerance and priority |
| 4. Asia High/Low | `confirmed` | Completed 1H candles with local `OpenTime` in the Asia interval | Maximum `High` and minimum `Low` recorded | Missing required data returns `data_unavailable` | No | Data completeness policy for non-nominal sessions |
| 5. London High/Low | `confirmed` | Completed 1H candles with local `OpenTime` in the London interval | Maximum `High` and minimum `Low` recorded | Missing required data returns `data_unavailable` | No | Data completeness policy for non-nominal sessions |
| 6. Preparation | `confirmed` semantics; `unresolved` runtime input | Clock and same-session evidence of completed Steps 1 and 2 | Completion in `[08:00, 08:30)` satisfies timing; clock alone does not | Incomplete at 08:30 makes `D` no-trade and blocks Step 3 without late recovery | Yes for Steps 1/2 | Completion-evidence contract, calendar eligibility and runtime prerequisite/metadata reconciliation |
| 7. Trading-window start | `confirmed` | Clock in `America/Bogota` | `waiting` before 08:30 | Entry prohibited before start | No for timezone/DST | None for timezone/DST |
| 8. Liquidity sweep | `confirmed` conceptually | Directionally relevant marked high/low and price | Price exceeds the selected High or goes below the selected Low; remaining details human-approved | Without the mandatory take, no downstream setup is valid | Yes | Level priority, rejection, pre-08:30 validity |
| 9. Move to 5M | `confirmed` | Approved sweep, 5M data | 5M review enabled | Blocks inversion without sweep | No | Data alignment |
| 10. 5M inversion OR | `confirmed` | Approved liquidity take and 5M structure/FVG evidence | First occurring Structural Change/ChoCH/MSS `OR` IFVG validated | Cannot precede the liquidity take and does not satisfy continuation FVG | Yes | Swing algorithm, strong/decisive threshold and IFVG geometry |
| 11. 5M close | `confirmed` | 5M candle close | Close beyond required level validated | Wick alone is `failed`; open candle is `waiting` | Yes for marginal close | Minimum distance |
| 12. Continuation FVG | `confirmed` + `human_validation_required` | Three 5M candles after the Step-4 trigger | Separate directional, clear FVG validated | Without this mandatory gate, Step 6 is not enabled | Yes | Minimum size, quality and lifecycle |
| 13. Move to 1M | `confirmed` | Approved FVG, 1M data | 1M review enabled | Blocks entry if data absent | No | Data alignment |
| 14. Corrective retracement | `confirmed` + `human_validation_required` | Approved Step-5 FVG and countertrend 1M candles | Pullback toward/into the relevant 5M FVG identified | Without the pullback/FVG interaction, no realignment gate | Yes | Interaction depth, correction boundaries and timeout |
| 15. 1M realignment | `confirmed` + `human_validation_required` | Corrective microstructure | Candidate realignment identified | No realignment means no entry | Yes | Corrective swing algorithm |
| 16. Corrective swing break | `confirmed` | Entry swing and closed 1M candle | Body closes beyond corrective swing | Wick/open candle does not pass | Yes for swing selection | Marginal close distance |
| 17. Entry | `confirmed` + `unresolved` | All earlier stages approved | `human_validation_required` | No automatic order creation | Yes | Order type, timing, slippage, attempts |
| 18. Stop Loss | `confirmed` conceptually + `human_validation_required` | Latest relevant structural 5M HL and entry | Human validates SL below/beyond the HL wick | Missing/ambiguous structural SL blocks management automation | Yes | HL algorithm, exact offset, spread and costs |
| 19. Break Even | `confirmed` conceptually + `human_validation_required` | Entry, London High, Asia High and observable price | On exact quote-level touch of the first encountered favorable session High, SL moves to entry | Not evaluated before entry; same-candle intrabar ordering may be unobservable | Yes | Data resolution, costs, universality and later management |
| 20. Take Profit | `confirmed` conceptually + `human_validation_required` | London High, Asia High and position state | Immediate target follows the first encountered relevant session High; one reviewed case exits completely on exact quote-level touch | No universal final-exit rule is established | Yes | First-target/final-TP relation, data resolution, edge cases, ratio and partials |
| 21. Trading-window end | `confirmed` | Clock in `America/Bogota` | Pre-entry workflow is outside `[08:30, 11:00)` at or after 11:00 | New or unfinished entry setup expires at 11:00 | No for timezone/DST | Post-entry management remains separately unresolved |
| 22. Risk controls | `confirmed` max trade risk + open controls | Entry, Stop Loss, sizing inputs | At most 1% risk after human validation | Missing sizing/control data blocks execution | Yes | Position size, daily limit, kill switch |

## 8. Sell workflow

Solo se incluyen relaciones direccionales expresamente documentadas. No se completan objetivos, Stop Loss ni gestión por simetría automática.

| Step | Rule status | Required inputs | Evaluation result | Blocking behavior | Human validation requirement | Open variables |
|---:|---|---|---|---|---|---|
| 1. 4H HH/LL context | `confirmed` + `human_validation_required` | Closed 4H candles | Direction inherited from the latest confirmed break, active structural pair, scenario and permitted direction reviewed | Blocks all lower stages | Yes | Initial-anchor selection, historical scan termination, other structural-point coordinates and retracement boundaries |
| 2. Breakout/Wickfill/Fakeout | `confirmed` conceptually | 4H context and relevant level | One classification recorded | Unclassified context blocks | Yes | Exact geometries and tolerances |
| 3. Relevant liquidity | `confirmed` + `human_validation_required` | Session levels and 1H/4H context | Asia/London extrema and structural coincidences recorded separately from 4H bootstrap | Missing levels block sweep evaluation | Yes | Structural-point algorithm, coincidence tolerance and priority |
| 4. Asia High/Low | `confirmed` | Completed 1H candles with local `OpenTime` in the Asia interval | Maximum `High` and minimum `Low` recorded | Missing required data returns `data_unavailable` | No | Data completeness policy for non-nominal sessions |
| 5. London High/Low | `confirmed` | Completed 1H candles with local `OpenTime` in the London interval | Maximum `High` and minimum `Low` recorded | Missing required data returns `data_unavailable` | No | Data completeness policy for non-nominal sessions |
| 6. Preparation | `confirmed` semantics; `unresolved` runtime input | Clock and same-session evidence of completed Steps 1 and 2 | Completion in `[08:00, 08:30)` satisfies timing; clock alone does not | Incomplete at 08:30 makes `D` no-trade and blocks Step 3 without late recovery | Yes for Steps 1/2 | Completion-evidence contract, calendar eligibility and runtime prerequisite/metadata reconciliation |
| 7. Trading-window start | `confirmed` | Clock in `America/Bogota` | `waiting` before 08:30 | Entry prohibited before start | No for timezone/DST | None for timezone/DST |
| 8. Liquidity sweep | `confirmed` conceptually | Directionally relevant marked high/low and price | Price exceeds the selected High or goes below the selected Low; remaining details human-approved | Without the mandatory take, no downstream setup is valid | Yes | Level priority, rejection, pre-08:30 validity |
| 9. Move to 5M | `confirmed` | Approved sweep, 5M data | 5M review enabled | Blocks inversion without sweep | No | Data alignment |
| 10. 5M inversion OR | `confirmed` | Approved liquidity take and 5M structure/FVG evidence | First occurring Structural Change/ChoCH/MSS `OR` IFVG validated | Cannot precede the liquidity take and does not satisfy continuation FVG | Yes | Swing algorithm, strong/decisive threshold and IFVG geometry |
| 11. 5M close | `confirmed` | Bullish-structure swing and 5M close | Candle body closes beyond relevant swing | Wick alone is `failed`; open candle is `waiting` | Yes for swing selection/marginal close | Pivot and minimum distance |
| 12. Continuation FVG | `confirmed` + `human_validation_required` | Three 5M candles after the Step-4 trigger | Separate clear FVG favors the new sell-side move | Without this mandatory gate, Step 6 is not enabled | Yes | Minimum size, quality and lifecycle |
| 13. Move to 1M | `confirmed` | Approved FVG, 1M data | 1M review enabled | Blocks entry if data absent | No | Data alignment |
| 14. Corrective retracement | `confirmed` + `human_validation_required` | Approved Step-5 FVG and countertrend 1M candles | Pullback toward/into the relevant 5M FVG identified | Without the pullback/FVG interaction, no realignment gate | Yes | Interaction depth, correction boundaries and timeout |
| 15. 1M realignment | `confirmed` + `human_validation_required` | Corrective microstructure | Candidate sell realignment identified | No realignment means no entry | Yes | Corrective swing algorithm |
| 16. Corrective swing break | `confirmed` | Entry swing and closed 1M candle | Body closes beyond corrective swing | Wick/open candle does not pass | Yes for swing selection | Marginal close distance |
| 17. Entry | `confirmed` + `unresolved` | All earlier stages approved | `human_validation_required` | No automatic order creation | Yes | Order type, timing, slippage, attempts |
| 18. Stop Loss | `confirmed` conceptually + `human_validation_required` | Latest relevant structural 5M LH and entry | Human validates SL above/beyond the LH wick | Missing/ambiguous structural SL blocks management automation | Yes | LH algorithm, exact offset, spread and costs |
| 19. Break Even | `confirmed` conceptually + `human_validation_required` | Entry, London Low, Asia Low and observable price | On exact quote-level touch of the first encountered favorable session Low, SL moves to entry | Not evaluated before entry; same-candle intrabar ordering may be unobservable | Yes | Data resolution, costs, universality and later management |
| 20. Take Profit | `confirmed` conceptually + `human_validation_required` | London Low, Asia Low and position state | Immediate target follows the first encountered relevant session Low; one reviewed case exits completely on exact quote-level touch | No universal final-exit rule is established | Yes | First-target/final-TP relation, data resolution, edge cases, ratio and partials |
| 21. Trading-window end | `confirmed` | Clock in `America/Bogota` | Pre-entry workflow is outside `[08:30, 11:00)` at or after 11:00 | New or unfinished entry setup expires at 11:00 | No for timezone/DST | Post-entry management remains separately unresolved |
| 22. Risk controls | `confirmed` max trade risk + open controls | Entry, Stop Loss, sizing inputs | At most 1% risk after human validation | Missing sizing/control data blocks execution | Yes | Position size, daily limit, kill switch |

## 9. 4H context

Preparation begins at 08:00 `America/Bogota`, coinciding with the close of the 4H candle used for the review. Only information observable through that closed candle may be used. The mentor reviews 4H to read the structure already developed, determine the direction permitted for the later setup and filter lower-timeframe oscillations/noise; 1M/5M internal movement does not independently create 4H structural points.

Direct human review of Video 2 approximately `05:00–05:10` and `09:55`, and Video 3 approximately `06:15–06:35`, confirms that valid H4 structural state can persist from `D` to `D+1` and later sessions until a qualifying structural transition replaces or invalidates it. The mentor retains established HH+HL or LL+LH points when the prior structural extreme has not actually been broken; ordinary internal movement and the calendar boundary do not recalculate them. At the daily 08:00 review, the just-closed H4 candle is evaluated against those existing references. A qualifying closed body break may roll the pair under the existing structural lifecycle; this normal evolution is not a new human bootstrap selection. The reviewed candle alone does not define all structure, and an open H4 candle cannot change the historical structural state. Breakout, Wickfill and Fakeout are reviewed relative to this context, but their unresolved geometry and classification precedence remain human-validated.

The human-selected initial anchor supplies causal bootstrap evidence and remains auditable provenance even when later confirmed transitions move the active pair forward. It need not remain one of the two current operational levels. Neither the anchor evidence nor a valid active pair expires merely because Asia/London liquidity is marked again for a new session: those session extrema and the `NQ-TIME-003` preparation-completion assertion have their own daily scope. The daily 08:00 preparation lifecycle therefore does not restart the H4 structural lifecycle or require another human anchor solely because `SessionDate` changed.

Cross-session structure is a replay concept, not mutable state hidden inside an `IReplayRuleEvaluator`. A future implementation must expose the accepted bootstrap evidence and derived structural state through causal replay inputs, observations or a deterministic replay-scoped fold; evaluators remain deterministic and stateless. At `AsOfUtc = T`, the state may use only human evidence observable by `T`, closed structurally relevant candles observable by `T` and prior causal replay state available by `T`. A later candle or human assertion cannot rewrite the result at `T`. This documents strategy lifetime and causality, not a runtime state-storage or input contract.

When facing a clean chart, the mentor visually/contextually starts from an evident High or Low that originated the current dominant directional leg and reconstructs the recent push, retracement, structural break, next push and current structure. In the reviewed bearish example (Video 2 approximately `09:50–11:05`), he selects an evident upper origin for the dominant bearish push, calls the direction clear, and ignores smaller internal pauses. The bullish mirror uses an evident lower/origin structure for the current bullish leg. These observations confirm the initial-visible-anchor concept, but visual clarity, dominance and the currently visible chart do not define exact swing geometry, prominence, price distance or a tie-break between plausible extremes. Statements such as “I already see it clearly” and “I will draw from here” do not define a machine-reproducible selector over arbitrary raw 4H candles. Viewport candle count, zoom, screen width, the highest/lowest visible candle and arbitrary loaded history must not become deterministic strategy inputs.

The mentor defines no fixed candle count, day count, session count, time window, pivot period or numeric lookback formula for this bootstrap. He does not scroll indefinitely backward in the reviewed clean-chart example. `H4_FIXED_LOOKBACK_RULE = CONFIRMED_NONE`; this excludes invented fixed or ATR-derived windows, but it does not define an unbounded historical scan or its reproducible termination boundary. The point at which the first raw-history scan stops is `H4_INITIAL_HISTORICAL_SCAN_TERMINATION = HUMAN_VALIDATION_REQUIRED`.

Once valid structural references are already established, direction inherits from the latest confirmed strict body-close structural break: a formally closed `Close > prior structural HH` establishes bullish context, while a formally closed `Close < prior structural LL` establishes bearish context. Wick-only penetration and equality do not establish that break, and current candle colors alone do not infer trend. After an upper break, the resulting HH-side extreme and the originating valley/turn that supplied the HL-side reference become the active pair; after a lower break, the resulting LL-side extreme and originating peak/turn that supplied the LH-side reference become the active pair. Internal oscillations without another qualifying structural break do not replace the pair. Older points remain auditable, but operational context compresses to that latest HH+HL or LL+LH pair until structure changes again. This compression limits the current operational view after references exist; it does not select the first raw-history anchor or scan boundary.

This post-anchor progression does not solve initialization circularity: “find the latest structural level that was broken” is deterministic only after the historical structural levels being compared already exist. Scanning backward for the first HH/HL or LL/LH pair is circular for the same reason unless its first anchor is independently established. The source supplies neither a raw-candle selector for that first anchor nor a deterministic stopping boundary for the initial scan. A first validated pair can be confirmed and rolled forward from supplied valid references, but cannot be inferred from completely unlabeled arbitrary history by this rule alone. Asia/London session highs/lows, weekly open and previous-day extrema remain operational or session context references and are not mandatory H4 bootstrap seeds.

Direct human review of Video 2 approximately `05:45–06:10` and `11:10–11:35`, with the separate wick-protection evidence in Video 3 approximately `07:20–07:35`, resolves the numeric coordinates of a *human-selected* initial 4H source candle. A High-side structural anchor has `StructuralPrice = max(Open, Close)` and `ProtectionAnchor = High`; a Low-side anchor has `StructuralPrice = min(Open, Close)` and `ProtectionAnchor = Low`. The body edge is the structural break-comparison level; the wick extreme is separate protection evidence, not an executable Stop Loss price. Both coordinates describe one reviewed structural vertex, even when their prices differ. These single-source-candle coordinates do not replace the separate aggregation rules for an already selected multi-candle HL/LH turn.

Video 3 approximately `07:40–07:46` supports preserving the exact human-selected source candle/event from which the directional push begins. That source identity fixes the accepted initialization boundary; it does not make the candle the exclusive owner of its horizontal structural price. If candles A and B both have High-side body edge `100` and the reviewer selects B as the bearish-push source, `StructuralPrice = 100` is one shared exact-price level while B is the source event. No first/last equal-price tie-break or automatic final-candle-of-cluster selector follows. Source-candle time and the later time at which human selection becomes observable to replay are distinct: a future source-backed observation must be invisible before its causal observation time under `StrategyReplayContext.AsOfUtc`, even when the selected candle is historical. With a valid supplied anchor, later closed-candle structural processing may begin from that operational boundary; older candles remain available as audit history. The anchor observation would attest the human choice, not subsequent trend, Breakout, candidate validation or `NQ-H4-001` passage.

The complete replay input contract is still open. The session boundary alone does not expire accepted initialization evidence or require its re-selection: derived valid H4 state persists across sessions. Normal confirmed-break pair rollover is separate from correcting or superseding a human assertion. The source still does not decide how a mistaken anchor is corrected, how duplicate or conflicting human anchors for one initialization scope are reconciled, or how structure is reinitialized after `StructureInvalidated`; that invalidation ends the previous same-side state without automatically reversing direction, choosing a new anchor, reconstructing opposite structure or reusing the old anchor. No `RuleEvaluationResult` is assigned when the required initialization evidence is absent. The canonical auxiliary-input pattern can carry a source-backed human choice, but no H4 observation type, field layout, correction/conflict policy or missing-input runtime mapping is established here.

This section governs the Step-1 4H context. The causal swing-validation rule below also applies to validated 1H structural points used by the documented target fallback; it does not make candidate detection or exact price placement deterministic.

### 9.1 Confirmed structural concepts

- Bullish confirmation progresses as `prior structural High → relevant retracement candidate → subsequent body-close break above that High/new HH → prior relevant retracement becomes confirmed HL`.
- Directly demonstrated bearish confirmation progresses as `prior structural LL area → retracement candidate high remains provisional → subsequent formally closed Close below the prior LL/new LL → preceding candidate high becomes confirmed LH from that close AsOfUtc`.
- `HH` is a structural High above the previous structural High. On the relevant 1H or 4H structure, a wick above is insufficient: `Candle.Close > previous structural High` confirms the bullish structural break and the new HH only after that candle formally closes.
- `LL` is a structural Low below the previous structural Low. On the relevant 1H or 4H structure, a wick below is insufficient: `Candle.Close < previous structural Low` confirms the bearish structural break and the new LL only after that candle formally closes.
- After an HH, the relevant low of the slowdown/retracement is provisional. It becomes a confirmed `HL` only when the later impulse originating from that candidate produces a formally closed body above the previous HH on the same relevant timeframe and forms the next HH. A rebound that does not achieve that body-close break does not validate the candidate as structural HL.
- After a prior LL, once the bearish impulse stops producing new lows, the first bullish candle that begins the upward displacement starts the bearish correction. If that bullish candle also prints a lower `Low`, the new price extreme takes precedence: update the correction-origin floor to that candle's `Low`, then start the correction only when the same candle closes bullish. This start is observable only from candles available through the current `AsOfUtc`; other scan-boundary inclusivity remains unresolved.
- When the candle producing the current HH/LL is distinct from the first opposite-body correction-start candle, that extreme candle remains in the preceding bullish/bearish impulse and outside the new correction. A distinct HH candle precedes the first bearish-body member; a distinct LL candle precedes the first bullish-body member. Printing the extreme alone does not start the correction. If the pre-start extreme candle is an exact doji, it likewise remains at the prior impulse's culmination boundary and outside the correction; only the following qualifying opposite body starts the turn. This pre-start exclusion does not change membership after a turn starts or the confirmed same-candle reset-and-start cases.
- After an HH/LL extreme, no correction turn exists while waiting for the first opposite-direction body. Every intervening candle is excluded, whether it is a same-direction body, an exact doji, or a contained candle in the HH/LL exhaustion/consolidation area. A waiting candle with a strict new `High > current ceiling` in bullish structure or `Low < current floor` in bearish structure extends that impulse's price extreme and updates the ceiling/floor under existing rules without starting a correction or automatically validating HH/LL. A waiting candle without a new extreme is also excluded. The same exclusion applies after `ResetAndAwaitCorrectionStart`: the reset candle and all later non-opposite bodies stay outside until the first bearish body (`Close < Open`) starts a bullish-structure correction or the first bullish body (`Close > Open`) starts a bearish-structure correction. These exact comparisons add no minimum body size, epsilon, tick, ATR or percentage threshold; near-doji treatment outside the exact comparison remains unresolved.
- The updated correction-origin floor is a price extreme, not a validated structural LL. `Low < prior LL` alone never bypasses the formally closed body-break requirement for structural LL validation.
- During that correction, the relevant candidate high remains provisional while the prior LL has no confirmed break. The correction ends and the candidate becomes a confirmed `LH` only when a formally closed candle has `Close < prior LL structural level` on the same relevant timeframe and forms the next LL. A wick-only penetration, an open candle or a decline without that close does not validate the candidate; validation exists only from the causal close `AsOfUtc`.
- For bullish candidate-HL selection, once the bullish impulse stops producing higher Highs, the first contrary/bearish candle starts the downward correction. If that candle also prints a `High` above the current correction-origin ceiling, the new higher extreme takes precedence: update the ceiling to that candle's `High`, then start the correction only when the same candle closes bearish. The correction did not start before that new extreme. This does not infer a finer intrabar path.
- The updated correction-origin ceiling is a price extreme, not a validated structural HH. `High > prior structural HH` alone never bypasses the formally closed body-break requirement for structural HH validation. This is the confirmed inverse of the bearish-side bullish-candle/new-low floor rule, without merging either origin extreme with structural HH/LL validation.
- During an active bullish correction with a valid prior structural HH, `High == prior HH`, `Close == prior HH`, and `High > prior HH` with `Close <= prior HH` do not validate the candidate HL. Absent an independent reset/invalidation event, that closed candle stays in the same candidate turn; its OHLC remains eligible for the selected turn's body-price and protection-anchor geometry, and later candles may deepen or replace the candidate. Only a formally closed `Close > prior HH` validates the candidate and excludes the causal confirming candle from that turn. The prior structural HH is a validation reference, not necessarily the current correction-origin ceiling: `High > current correction-origin ceiling` still resets the turn even if `Close <= prior HH`.
- A new direct human review of Video 2 approximately `03:05–03:40` and `10:55`, followed by explicit human clarification, resolves the bullish compound event when the same formally closed candle has both `Close > prior structural HH` and `High > current correction-origin ceiling`. The strict close validates the existing candidate HL instead of letting the generic new-High reset discard it. The old candidate turn closes successfully; the confirming candle is excluded from its `StructuralPrice` and `ProtectionAnchor`. That candle's new upper extreme updates the current origin ceiling and supplies the new HH-side reference, so the operational pair rolls forward to new HH plus newly validated HL from the causal close; older structure remains auditable. This does not infer an intrabar order or a new HH-coordinate algorithm. Neither `ResetAndStartNewCorrection` nor `ResetAndAwaitCorrectionStart` describes the old candidate's outcome in this compound case.
- New direct human review of Video 2 approximately `01:40–01:55`, `03:05–03:40`, `04:45–05:25`, `05:40–06:15`, `06:40–07:35` and `10:55–11:05`, followed by explicit human clarification, confirms the bearish compound mirror. With an active candidate LH, one formally closed candle satisfying both `Close < prior structural LL` and `Low < current correction-origin floor` validates that old LH instead of discarding it under generic new-Low reset. The confirming candle is excluded from the old LH turn's `StructuralPrice` and `ProtectionAnchor`; its lower extreme updates the current origin floor and supplies the new LL-side reference. From that causal close, the operational pair becomes new LL plus newly validated LH, while older structure remains auditable. No new LL-coordinate algorithm or intrabar order is inferred.
- After a candidate-confirming close establishes the next HH/LL, determine *next*-turn membership from that same candle's exact body, including in either confirmation-plus-origin-extension event. A bearish body (`Close < Open`) starts the next bullish-structure correction; a bullish body (`Close > Open`) starts the next bearish-structure correction. The confirming candle is its first and only initial member, included exactly once in that new turn's geometry while excluded from the validated old candidate turn. If its body is not corrective (including exact `Close == Open`), the next correction is not active: the valid new structural side is `AwaitingCorrectionStart` with an empty new turn. The same first-opposite-body start rule supplies the next-turn outcome when a confirming close does not extend the distinct origin ceiling/floor. For example, `prior HH = 100`, origin ceiling `102`, `Open = 104`, `High = 105`, `Close = 103` validates the old HL, establishes the new HH and starts the next bullish correction with this bearish-body candle. This adds no near-doji tolerance and does not route successful old-candidate validation through generic `ResetAndStartNewCorrection` or `ResetAndAwaitCorrectionStart`.
- Human review confirms the ordered structural levels of a valid pair: bullish `prior validated HL < prior structural HH`, bearish `prior structural LL < prior validated LH`. One `Close` therefore cannot simultaneously satisfy bullish `Close > prior HH` and `Close < prior validated HL`, or bearish `Close < prior LL` and `Close > prior validated LH`. Candidate confirmation plus opposite-point structural invalidation is not reachable in valid structure. A wick beyond the HH followed by `Close < prior HL`, or a wick below the LL followed by `Close > prior LH`, is not body-close confirmation; existing invalidation semantics govern, including invalidation dominance when an origin-extreme reset also occurs. No runtime outcome for an impossible close-vs-close collision is inferred.

For a supplied valid active candidate turn, the causal closed-candle boundary has these deterministic outcomes; the old candidate's result precedes classification of any next turn:

| Observation at causal close | Old candidate / old turn | Structural side and active pair | Next correction |
| --- | --- | --- | --- |
| No confirmation, reset or invalidation | Remains provisional; current in-range candle belongs to the old turn | Existing valid pair | Continue active turn |
| Strict confirming close without origin-extreme extension | Validated; confirming candle excluded from old geometry | New HH + validated HL, or new LL + validated LH under existing body-close validation | Exact corrective body starts a new turn with this candle only; otherwise `AwaitingCorrectionStart` with an empty new turn |
| Strict confirming close plus origin-extreme extension; corrective body | Validated; confirming candle excluded from old geometry | New HH + validated HL, or new LL + validated LH | Active next turn containing exactly the confirming candle |
| Strict confirming close plus origin-extreme extension; non-corrective or exact-doji body | Validated; confirming candle excluded from old geometry | New HH + validated HL, or new LL + validated LH | `AwaitingCorrectionStart`; new turn empty |
| No confirmation; strict origin-extreme reset; corrective body | Discarded; reset candle excluded from old geometry | Existing valid structural side; ceiling/floor updated, without automatic HH/LL validation | Active new turn containing exactly the reset candle |
| No confirmation; strict origin-extreme reset; non-corrective or exact-doji body | Discarded; reset candle excluded from old geometry | Existing valid structural side; ceiling/floor updated, without automatic HH/LL validation | `AwaitingCorrectionStart`; new turn empty |
| Strict opposite-point invalidating close, with or without origin-extreme reset | Destroyed; invalidating candle excluded from old geometry | `StructureInvalidated`; new opposite impulse evidence, no automatic validated point | No correction on the invalidated former side |

The strict confirmation-plus-opposite-point-invalidation row is absent because the valid-pair ordering makes it unreachable. `STRUCTURAL_CANDIDATE_TURN_BOUNDARY = READY_FOR_DETERMINISTIC_IMPLEMENTATION` for an already supplied valid structural side, references and active candidate turn: every reachable closed-candle disposition above is specified. This scoped readiness does not select an initial anchor or reconstruct all structural points from arbitrary history.
- The bearish mirror uses the prior structural LL as validation reference, distinct from the current correction-origin floor. `Low == prior LL`, `Close == prior LL`, and `Low < prior LL` with `Close >= prior LL` do not validate the candidate LH. Absent an independent reset/invalidation event, the candle stays in the same turn, contributes eligible OHLC to its selected geometry, and a later higher candidate may replace the prior one. Only a formally closed `Close < prior LL` validates the LH and excludes the confirming candle from that old turn; `Low < current correction-origin floor` ordinarily resets when no same-candle strict candidate confirmation occurs. A wick crossing the prior structural reference alone is not the formal structural break for candidate validation. No intrabar ordering is inferred.
- An exact doji has `Close == Open`, therefore has neither bullish nor bearish body direction and does not start a correction by itself. In bullish structure, wait for the next qualifying bearish-body candle; in bearish structure, wait for the next qualifying bullish-body candle. The decision at the doji's `AsOfUtc` remains “correction not started” and cannot be rewritten using later data or another timeframe.
- If an exact doji prints a `High` above the current correction-origin ceiling or a `Low` below the current correction-origin floor, update that origin extreme under the confirmed precedence rule but do not start directional correction. Extreme update and correction start remain separate. This does not turn the doji into an opposite-direction candle or validate a structural HH/LL.
- This exact equality rule does not define near-doji or small-body treatment. No epsilon, tick distance or other numeric threshold for `abs(Close - Open)` is source-supported.
- An opening/quotation gap, defined only as `previous.Close != current.Open`, has no independent structural event semantics and no size threshold. A gap down does not start a bullish-structure correction without a bearish body, and a gap up does not start a bearish-structure correction without a bullish body.
- Gap opens do not replace the confirmed terminal tests: bullish reset still requires `High > current correction-origin ceiling`, bearish reset still requires `Low < current correction-origin floor`, bullish invalidation still requires formally closed `Close < prior validated HL`, and bearish invalidation still requires formally closed `Close > prior validated LH`. An `Open` beyond a level is insufficient by itself.
- Gap existence alone does not terminate or fragment an active correction turn. Do not interpolate a path between `previous.Close` and `current.Open`, synthesize candles, or reinterpret the discontinuity as FVG/IFVG evidence.
- While no terminal/reset event has occurred, every in-range intermediate candle remains in the same active bullish correction turn. This includes bearish and bullish bodies, exact dojis, internal oscillations, consolidations and multiple candles without clear displacement. Body-color alternation does not fragment the turn or create parallel HL candidates. An exact doji cannot start a correction, but after the correction is active it may remain inside this same turn.
- A strict `High > current correction-origin ceiling` during an active bullish correction resets that correction when the same candle does not confirm the candidate HL. The prior candidate then ceases to be current and the ceiling updates to the new higher price extreme before the same closed candle's body is classified. If `Close < Open`, that non-confirming reset candle is excluded from the old correction and included as the first member of the new bullish-correction turn; no second bearish candle is required. If `Close == Open` or `Close > Open`, the reset occurs but the new correction does not start. A reset alone does not validate a structural HH: the independent formally closed body-break rule still applies. When a strict `Close > prior HH` also occurs, the confirmed candidate-validation collision above governs the old candidate; exact corrective body determines next-turn inclusion and other bodies leave `AwaitingCorrectionStart`. No finer intrabar path is inferred.
- A formally closed candle with `Close < prior validated HL structural reference` invalidates the active bullish structure. The correction cannot produce or continue the same candidate HL after that causal close. The invalidation candle is excluded from the previous correction turn and previous candidate-HL geometry, cannot retrospectively alter its `StructuralPrice` or `ProtectionAnchor`, and becomes evidence for the new bearish impulse without automatically becoming a validated structural LL. `Close == prior HL` does not invalidate under this rule, and `Low < prior HL` with `Close >= prior HL` is wick-only penetration and also does not invalidate.
- Until the causal `Close > prior HH` confirmation or one of those terminal/reset events occurs, the candidate HL can continue to be replaced by a deeper low within the same correction turn. Preliminary/intermediate lows do not survive as HLs merely because body direction alternates.
- For candidate-HL selection inside that active correction, the candidate is the deepest low of the correction and must be the base from which the impulse that later produces the confirming body-close break above the prior HH originates.
- Small pauses of one or two candles inside that impulse do not create a new candidate HL.
- The bullish correction does not formally end merely because price rebounds. It ends when a formally closed candle has `Close > prior HH structural level`; only from that causal close `AsOfUtc` onward does the deepest candidate become a validated HL.
- Bearish correction start/end, distinct pre-start LL-candle exclusion, exact-doji no-start, bullish-candle/new-low precedence, correction-origin floor update, LH causal validation, prior-LL origin, complex candidate-LH selection, highest-high replacement and the selected-turn structural coordinate are directly confirmed, not inferred. Within one recognized bearish correction, a later higher candidate replaces a lower one and superseded intermediate highs do not survive. Candidate validation still occurs only from the causal formally closed `Close < prior LL` `AsOfUtc`. For the already selected LH turn, the structural price is the highest `Open`/`Close` body edge among its member candles; a higher wick does not redefine it. The full bearish window remains partially defined because near-doji treatment, other boundary inclusivity and broader turn segmentation are unresolved. The sell highest-wick/tail Stop Loss anchor is a separate risk/invalidation semantic, not the structural coordinate.
- For the selected candidate-HL turn, mark the structural reference at the lowest candle-body coordinate: the lowest `Open` or `Close` among the bodies belonging to that turn. An isolated wick does not define the structural level.
- If several included bodies share the same exact lowest `Open`/`Close`, they support one structural price level or horizontal structural zone. No first-candle, last-candle, timestamp or sequence-number ownership is required, and multiple supporting candles do not create multiple HL/LH points or fragment the structural point. “Zone” here defines no width, tolerance, epsilon, ticks, points or percentage band. Exact doji no-start and mirrored terminal membership are confirmed; near-doji thresholds and near-equal structural-zone tolerance remain unresolved.
- Structural price and protection geometry aggregate independently over the same selected turn. A lower/HL turn uses `StructuralPrice = min(min(Open, Close))` and `ProtectionAnchor = min(Low)`; an upper/LH turn uses `StructuralPrice = max(max(Open, Close))` and `ProtectionAnchor = max(High)`. Different candles may supply those two extrema without creating two structural points. The protection anchor is not the executable Stop Loss; offset, buffer, ticks/points, spread, costs and broker semantics remain unresolved.
- For a normal correction start with distinct extreme and opposite-body candles, pass only the correction turn beginning at that first opposite-body candle to candidate geometry. The preceding extreme candle's `Open`, `Close`, `High` and `Low` do not enter its `StructuralPrice` or `ProtectionAnchor` merely because that candle produced the HH/LL. A bearish new-High or bullish new-Low candle that itself starts the correction remains included under the separate same-candle precedence rule.
- The same geometry boundary excludes every candle observed while correction start is pending, including contained candles and new-extreme updates after `ResetAndAwaitCorrectionStart`. None of their `Open`, `Close`, `High` or `Low` contributes to the future candidate turn's `StructuralPrice` or `ProtectionAnchor`. Once the first opposite-body candle starts the turn, subsequent in-range bodies of either direction and exact dojis may remain members under the existing active-turn rules.
- Correction-turn lifecycle has three distinct conceptual dispositions: `ActiveCorrection`, `AwaitingCorrectionStart` and `StructureInvalidated`. Both inactive dispositions have an empty turn, but they are not interchangeable. `AwaitingCorrectionStart` requires a still-valid structural side and arises after a valid HH/LL context before its first opposite body, after an ordinary `ResetAndAwaitCorrectionStart`, or after a candidate-confirming close that starts no next correction; waiting candles remain excluded, and the first qualifying opposite body becomes the sole initial turn member. `StructureInvalidated` follows a closed body beyond the prior validated opposite structural point: the former side is no longer valid, the invalidation candle is excluded from the destroyed turn and belongs to the new opposite impulse context, but it establishes neither a validated opposite swing nor a complete opposite structural pair.
- Existing terminal-transition dispositions remain causal for their confirmed scenarios: `ContinueExistingCorrection` retains `ActiveCorrection` and appends the current candle; an ordinary non-confirming `ResetAndStartNewCorrection` produces `ActiveCorrection` with only the current candle in the new turn; `ResetAndAwaitCorrectionStart` destroys the old turn and produces `AwaitingCorrectionStart`; `InvalidateBullishStructure` and `InvalidateBearishStructure` destroy the old turn and produce `StructureInvalidated`. In the strict reset/invalidation collision, invalidation dominates and the resulting disposition is `StructureInvalidated`. Bullish or bearish candidate-confirmation plus origin extension validates the old candidate and cannot be represented as an ordinary reset/start or reset/await of that candidate; current runtime primitives do not yet compose this higher-level result, whose next-turn exact-body disposition is now specified. Both strict observations remain recorded without inferring intrabar chronology.
- `CorrectionStartTransition` applies only when a valid structural side exists, no correction turn is active, and the lifecycle disposition is `AwaitingCorrectionStart`. An empty turn or `IsCorrectionTurnActive == false` alone is insufficient. It cannot be applied to the invalidated former side; invalidation does not automatically reverse structure, start an opposite correction or validate an opposite candidate in the same frame. A later valid structural context must first be established under the existing structural/bootstrap rules. The exact reconstruction from arbitrary candles remains partially defined by initial-anchor selection, historical scan termination and candidate detection for other structural classes.
- Implementation boundary: body-direction and extreme observations, `CorrectionStartTransitionCalculator` and distinct causal `CorrectionTurnLifecycleResult` dispositions exist. The lifecycle calculator consumes start/terminal transitions and can include an existing-turn candle, but no current primitive selects active-turn membership from prior structural HH/LL non-confirmation together with independent reset/invalidation observations. `StructuralCandidateValidationCalculator` consumes a caller-selected turn and does not assemble membership automatically. No complete opposite-structure reconstruction is established.

Example: if three HL-turn body lower edges are `100`, `100` and `102`, the result is one `StructuralPrice = 100` with no A-vs-B owner. If the first two candle Lows are `98` and `97`, `ProtectionAnchor = 97` while structural price remains `100`. Exact equality creates the shared price level; a different price such as `100.01` is not merged without an independently defined tolerance.
- The causal confirming break candle is excluded from the old correction turn and its retrospective candidate scan; in the confirmed compound event, a corrective body can also make it the first member of the next turn. Its close ends the correction, confirms the new HH/LL and validates the preceding candidate from that observable `AsOfUtc`. Candles retrospectively identified as expansion from the prior turn toward the break belong to the impulsive leg; this phase label does not fragment the still-active correction into parallel turns before the causal boundary.
- A single expansive candle may both contain evidence of the impulse origin at the selected turn and produce the confirming close. It remains excluded from correction membership, but its origin evidence must not be discarded. For this case only, the structural HL/LH origin coordinate is the candle `Open`; the wick does not redefine it. The same candle's `Close` remains the causal validation event, so no synthetic intrabar chronology is required or permitted.
- After the recent structure is reconstructed, the active current pair is the latest confirmed HH and HL in bullish context, or the latest confirmed LL and LH in bearish context. Older points remain in the historical audit trail even when they are less relevant to the current decision.

For an already understood abstract sequence `Low A → High B → Low C → High D`, a causal formally closed body at `High D` above the valid prior upper structural reference `High B` confirms/inherits bullish direction. `High D` supplies the relevant HH-side outcome and the originating `Low C` turn supplies the relevant HL-side candidate or validated reference under the existing causal rules; older `A/B` history need not remain the operational active pair. This example does not claim that the engine can identify `A/B/C/D` from arbitrary raw candles without the initial human-approved anchor.

The source now materially clarifies retrospective correction windows in both directions. For bullish structure: prior upside extreme → first bearish correction candle → correction observations → impulsive expansion → formally closed body above the prior structural HH. For bearish structure, mirror the sequence from the prior downside extreme and first bullish correction candle through a formally closed body below the prior structural LL. Equality or wick-only penetration of the prior structural HH/LL does not confirm and, absent another terminal event, stays in the candidate turn with eligible OHLC geometry. A strict new origin extreme ordinarily resets the active correction and discards its candidate; the same non-confirming reset candle starts the new turn only when its body points in the corrective direction. In both confirmed confirmation-plus-extension collisions, the strict body close validates the old HL/LH while the new upper/lower extreme establishes the HH/LL-side reference. An exact corrective body includes the candle once in the next turn; otherwise the valid side awaits correction start with an empty new turn. A body close beyond the prior validated opposite structural point invalidates the active structure, excludes that candle from the destroyed turn and geometry, and starts opposite-impulse context. When reset and invalidation coexist, invalidation dominates. An updated origin extreme alone is not thereby promoted to a validated structural point, and no intrabar path is inferred. The causal confirming breakout candle remains excluded from old correction membership and correction-candidate scanning. Both windows remain partially defined because near-doji treatment beyond exact body comparisons, initial-anchor selection and historical scan termination are not supplied. The selected candidate-HL turn defines its lowest `Open`/`Close` price and the recognized candidate-LH turn uses the corresponding highest-body edge; an exact equal-coordinate tie remains one price level and requires no candle identity. In the special single-candle case, the confirming candle's `Open` is the structural HL/LH origin coordinate and its `Close` is the causal validation event; it still does not become a member of the old correction turn. This does not generalize over the existing multi-candle selected-turn rules. No fixed pivot or lookback exists and no near-doji or near-equal-zone tolerance is inferred; the buy/sell protection wicks remain separate from structural coordinates.

“Price slows down” remains a visual source criterion. It does not define candle count, minimum retracement, pivot width, percentage, ATR or candle-size thresholds. The source also does not define how to obtain the initial previous structural High/Low from an arbitrary raw 4H series without a human annotation or pre-seeded level. Full structural detection therefore remains `human_validation_required` and blocked for deterministic evaluation specifically by initial-anchor selection, despite the confirmed no-fixed-lookback rule and materially defined post-anchor progression.

### 9.2 Retrospective confirmation and historical replay

HL/LH confirmation is retrospective and causal but must not leak future information. At replay time `T1`, before the subsequent 1H/4H HH/LL break candle has formally closed, the prior retracement remains candidate/unconfirmed and minor fluctuations remain non-structural even if the open candle temporarily trades beyond the level. From `T2`, the `AsOfUtc` at which the subsequent body-close break confirms the new HH/LL, the earlier relevant retracement may be recorded as the confirmed HL/LH. Historical evaluation at `T1` must not use the `T2` close or relabel the candidate as already confirmed; a future candle must never change what was considered confirmed at an earlier replay timestamp.

Historical automation of `NQ-H4-001` therefore requires reconstruction using only information observable through each `AsOfUtc`. Before the causal bullish or bearish close, the candidate remains unvalidated; later observations cannot alter an earlier historical evaluation or terminal-candle membership. The source defines both correction starts and causal ends, exact-doji neutral/no-start behavior, both origin-extreme precedence cases and their floor/ceiling updates, mirrored in-range membership including non-confirming prior-HH/LL equality and wick-only penetration, mirrored reset and invalidation semantics, invalidation-dominant collisions, terminal-candle membership, confirming-candle exclusion and validation role, the single-candle `Open` origin coordinate, exact equal-price structural identity, direction from the latest confirmed break and compression to the latest active pair. It also directly confirms the bearish prior-LL origin, complex candidate-LH selection, highest-high replacement and selected-turn highest-body-edge LH coordinate, while partially defining the bullish and bearish correction windows, candidate-HL selection and body anchoring. It does not define a complete detector: initial-anchor selection from arbitrary raw candles, historical scan termination, near-doji thresholds, near-equal-zone tolerance and coordinates for other structural-point classes remain unresolved or partial. No fixed lookback exists, so it must not be retained as an unresolved numeric-rule question.

| Structural concern | Status | Boundary |
|---|---|---|
| `STRUCTURAL_VALIDATION_RULE` | `CONFIRMED` | HL/LH validation is caused by a later formally closed body beyond the prior HH/LL; HH/LL also require that body-close break on the same relevant 1H/4H timeframe. |
| `LH_CAUSAL_VALIDATION` | `CONFIRMED` | A preceding candidate high becomes a validated LH/new LL only from a formally closed `Close < prior LL`; wick-only and open-candle penetration are insufficient. |
| `BEARISH_CORRECTION_ORIGIN` | `CONFIRMED` | The demonstrated bearish retracement starts from the current floor: normally the prior LL area, or the updated candle `Low` in the confirmed bullish-candle/new-low case. The floor is not automatically a validated structural LL. |
| `BEARISH_CORRECTION_START` | `CONFIRMED` | After the prior LL and cessation of new lows, the first exact bullish body (`Close > Open`) starts a bearish correction. In the confirmed confirmation-plus-extension event, that same corrective-body candle is the first member of the next turn; a non-corrective body leaves `AwaitingCorrectionStart`. Prior waiting candles remain excluded and no body-size threshold is inferred. |
| `DISTINCT_EXTREME_CANDLE_MEMBERSHIP` | `CONFIRMED_EXCLUDED_FROM_CORRECTION` | When the current HH/LL-producing candle and first opposite-body candle are distinct, the extreme candle belongs to the prior impulse and is excluded from the new correction turn and its geometry. |
| `PRE_START_EXACT_DOJI_AT_EXTREME` | `CONFIRMED_EXCLUDED_FROM_CORRECTION` | An exact-doji HH/LL candle before the first qualifying opposite body remains at the prior impulse's culmination boundary, outside the correction; it does not start a turn. |
| `PRE_START_WAITING_CANDLE_MEMBERSHIP` | `CONFIRMED_EXCLUDED_FROM_CORRECTION` | Before the first opposite-body start, all waiting candles, including same-direction bodies, exact dojis, contained candles and strict new-extreme updates, remain outside the future correction and its geometry; this also applies after `ResetAndAwaitCorrectionStart`. |
| `RESET_AND_AWAIT_LIFECYCLE_STATE` | `CONFIRMED` | `ResetAndAwaitCorrectionStart` destroys the active turn but leaves the same valid structural side in `AwaitingCorrectionStart`, distinct from structural invalidation. |
| `CORRECTION_START_TRANSITION_PRECONDITION` | `CONFIRMED` | A valid structural side, inactive correction and semantic `AwaitingCorrectionStart` disposition are all required; an empty/inactive turn alone is insufficient. |
| `INVALIDATED_STRUCTURE_CORRECTION_START_APPLICABILITY` | `CONFIRMED` | `CorrectionStartTransition` is not applicable to the invalidated former side; a later valid structural context must first be established without automatic reversal. |
| `BEARISH_CORRECTION_END` | `CONFIRMED` | The bearish correction ends only when a formally closed candle has `Close < prior LL structural level`. |
| `BULLISH_CANDLE_WITH_NEW_LOW_PRECEDENCE` | `CONFIRMED` | When the first bullish candle also prints a lower `Low`, the new lower extreme updates the origin floor before the candle's bullish close starts the correction. |
| `CORRECTION_ORIGIN_FLOOR_UPDATE` | `CONFIRMED` | The lower `Low` becomes the correction-origin price floor; it is not automatically a validated structural LL. |
| `CORRECTION_START_AFTER_UPDATED_FLOOR` | `CONFIRMED` | The correction starts from the updated floor only when that same candle closes bullish; it did not start before the new extreme. |
| `CORRECTION_ORIGIN_EXTREME_VS_STRUCTURAL_LL` | `CONFIRMED` | A correction-origin `Low` and a validated structural LL are distinct; the latter still requires the formally closed body-break rule. |
| `BEARISH_CANDLE_WITH_NEW_HIGH_PRECEDENCE` | `CONFIRMED` | When the first bearish candle also prints a higher `High`, the new higher extreme updates the origin ceiling before the candle's bearish close starts the correction. |
| `CORRECTION_ORIGIN_CEILING_UPDATE` | `CONFIRMED` | The higher `High` becomes the correction-origin price ceiling; it is not automatically a validated structural HH. |
| `CORRECTION_START_AFTER_UPDATED_CEILING` | `CONFIRMED` | The correction starts from the updated ceiling only when that same candle closes bearish; it did not start before the new extreme. |
| `CORRECTION_ORIGIN_EXTREME_VS_STRUCTURAL_HH` | `CONFIRMED` | A correction-origin `High` and a validated structural HH are distinct; the latter still requires the formally closed body-break rule. |
| `OPENING_GAP_STRUCTURAL_ROLE` | `CONFIRMED_NO_SPECIAL_ROLE` | `previous.Close != current.Open` has no independent structural meaning and does not create, validate, reset or invalidate a structural point by itself. No gap-size threshold applies. |
| `GAP_CORRECTION_START_BEHAVIOR` | `CONFIRMED_NO_SPECIAL_ROLE` | Correction start remains based on exact body direction: bearish body for bullish structure, bullish body for bearish structure; opening displacement alone does not start it. |
| `GAP_RESET_BEHAVIOR` | `CONFIRMED_EXISTING_EXTREME_RULE_GOVERNS` | Reset remains governed only by strict `High > ceiling` or `Low < floor`; `Open > ceiling` and `Open < floor` are not separate reset rules. |
| `GAP_BULLISH_INVALIDATION_BEHAVIOR` | `CONFIRMED_CLOSE_GOVERNS` | Bullish invalidation requires formally closed `Close < prior validated HL`; a gap/open below the HL with `Close >= HL` does not invalidate. |
| `GAP_BEARISH_INVALIDATION_BEHAVIOR` | `CONFIRMED_CLOSE_GOVERNS` | Bearish invalidation requires formally closed `Close > prior validated LH`; a gap/open above the LH with `Close <= LH` does not invalidate. |
| `GAP_TURN_MEMBERSHIP` | `CONFIRMED_NO_FRAGMENTATION_BY_GAP_ALONE` | An opening discontinuity alone neither terminates nor fragments an active correction turn; normal membership and terminal/reset rules continue to govern. |
| `H4_FIXED_LOOKBACK_RULE` | `CONFIRMED_NONE` | The source defines no fixed candles, days, sessions, time window, scan count, pivot period or numeric lookback formula. This does not define an unbounded scan boundary. |
| `H4_INITIAL_VISIBLE_ANCHOR_CONCEPT` | `CONFIRMED` | The visual review begins from an evident upper origin of the dominant bearish leg or lower/origin structure of the bullish mirror. |
| `H4_INITIAL_VISIBLE_ANCHOR_SELECTION` | `HUMAN_VALIDATION_REQUIRED` | Visual clarity and dismissal of minor pauses define no exact swing geometry, prominence, price distance or tie-break; viewport, zoom, screen width and loaded history cannot select the first anchor. |
| `H4_INITIAL_HISTORICAL_SCAN_TERMINATION` | `HUMAN_VALIDATION_REQUIRED` | No reproducible rule says how far arbitrary raw 4H history must be searched before accepting the first anchor; stopping after a visually identifiable pair is not a raw-history algorithm. |
| `H4_INITIAL_ANCHOR_STRUCTURAL_COORDINATE` | `CONFIRMED` | For the human-selected source candle, High-side `StructuralPrice = max(Open, Close)` and Low-side `StructuralPrice = min(Open, Close)`; the wick does not redefine the break-comparison level. |
| `H4_INITIAL_ANCHOR_PROTECTION_COORDINATE` | `CONFIRMED` | The same High-side source candle supplies `ProtectionAnchor = High`; the Low-side source supplies `ProtectionAnchor = Low`. This remains distinct from structural price and executable Stop Loss. |
| `H4_INITIAL_ANCHOR_SOURCE_EVENT_IDENTITY` | `CONFIRMED` | Preserve the exact reviewer-selected source candle/event as the causal initialization boundary without making it sole owner of its price level or automatically selecting it from a cluster. |
| `H4_INITIAL_ANCHOR_PRICE_LEVEL_IDENTITY` | `CONFIRMED_PRICE_LEVEL` | Exact equal body-based structural prices share one horizontal level even if one particular candle/event is the human-selected initialization source. |
| `H4_INITIAL_ANCHOR_CAUSAL_OBSERVABILITY` | `CONFIRMED` | Historical source-candle time differs from the human assertion's causal observation time; no replay context before that observation time may use the choice. |
| `H4_STRUCTURAL_STATE_LIFETIME` | `CONFIRMED` | Valid derived HH+HL or LL+LH state persists across trading sessions until qualifying structural transition or invalidation; the calendar day is not its expiry boundary. |
| `H4_DAILY_STRUCTURAL_REINITIALIZATION` | `CONFIRMED_NONE` | Daily 08:00 preparation reviews the existing valid state and newly closed H4 candle; it does not bootstrap raw history again merely because a new day began. |
| `H4_ACTIVE_PAIR_CROSS_SESSION_PERSISTENCE` | `CONFIRMED` | The active pair remains operational across sessions while no qualifying transition replaces or invalidates it; internal price movement alone does not roll it. |
| `H4_DAILY_0800_EXISTING_STATE_REVIEW` | `CONFIRMED` | The formally closed 08:00 H4 candle is reviewed against existing structural references, separately from the session-scoped Asia/London levels and preparation assertion. |
| `H4_INITIAL_ANCHOR_SESSION_EXPIRATION` | `CONFIRMED_NONE` | A session/date boundary alone neither expires accepted bootstrap evidence nor mandates a new human anchor. |
| `H4_INITIALIZATION_EVIDENCE_LIFETIME` | `CONFIRMED` | The initial human selection remains auditable provenance when derived active references move forward; it is not necessarily an active operational level forever. |
| `H4_ACTIVE_STATE_NORMAL_SUPERSESSION` | `CONFIRMED` | A qualifying confirmed structural progression can roll the active HH+HL or LL+LH pair under existing rules without replacing the human bootstrap assertion. |
| `H4_INITIAL_ANCHOR_LIFETIME` | `PARTIALLY_DEFINED` | Accepted bootstrap evidence does not expire per session and remains auditable; correction/supersession of mistaken human evidence and post-invalidation applicability remain open. |
| `H4_INITIAL_ANCHOR_REINITIALIZATION` | `UNRESOLVED` | Replacement of human evidence and the path after `StructureInvalidated` are not specified; normal pair rollover is separate. |
| `H4_POST_INVALIDATION_REINITIALIZATION` | `UNRESOLVED` | Invalidation ends the previous same-side state but defines no automatic reversal, opposite reconstruction, new human anchor or old-anchor reuse. |
| `H4_INITIAL_ANCHOR_CONFLICT_POLICY` | `UNRESOLVED` | No source-backed scope or precedence resolves two human-selected anchors targeting the same initialization context. |
| `H4_MISSING_INITIAL_ANCHOR_RUNTIME_STATUS` | `UNRESOLVED` | No audited mapping selects `human_validation_required`, `waiting` or `data_unavailable` when the input is absent. |
| `H4_HUMAN_SELECTED_ANCHOR_REPLAY_INPUT` | `PARTIALLY_DEFINED` | Coordinates, source-event identity, causal observability and no daily expiration are specified; correction/conflicts, post-invalidation reinitialization and missing-input status still block a complete runtime input contract. |
| `H4_DIRECTION_FROM_LATEST_CONFIRMED_BREAK` | `CONFIRMED` | The latest strict formally closed body break above/below an already-established structural reference supplies inherited bullish/bearish direction; equality and wick-only penetration do not. |
| `H4_ACTIVE_STRUCTURAL_PAIR_AFTER_CONFIRMED_BREAK` | `CONFIRMED` | An upper break leaves the HH-side outcome plus its originating HL-side turn active; a lower break leaves the LL-side outcome plus its originating LH-side turn active under existing causal validation rules. |
| `H4_HISTORY_COMPRESSION_TO_ACTIVE_PAIR` | `CONFIRMED` | Operational context retains the latest HH+HL or LL+LH pair until structure changes, while older points remain in the audit trail. |
| `H4_RAW_CANDLE_INITIALIZATION` | `HUMAN_VALIDATION_REQUIRED` | Arbitrary raw 4H history cannot reproducibly establish the first structural reference without a human-approved anchor and initial scan boundary. |
| `H4_FULL_DETERMINISTIC_BOOTSTRAP` | `PARTIALLY_DEFINED` | Post-anchor direction, active-pair compression and causal progression are materially defined, but initial-anchor selection blocks full deterministic bootstrap. |
| `NQ_H4_DIRECTION_BOOTSTRAP` | `PARTIALLY_DEFINED` | Direction is deterministic from the latest confirmed break only after valid structural references exist; raw-candle initialization remains human-validated. |
| `CANDIDATE_HL_SELECTION` | `PARTIALLY_DEFINED` | Deepest-low replacement, exact equal-price identity, non-confirming prior-HH membership, ordinary reset/invalidation and successful bullish confirmation-plus-extension are confirmed; near-equal tolerance, initial-anchor selection and historical scan termination still prevent a complete detector. |
| `BULLISH_CORRECTION_START` | `CONFIRMED` | The first exact bearish body (`Close < Open`) starts a correction after any same-candle higher `High` updates the origin ceiling. In the confirmed confirmation-plus-extension event, that same corrective-body candle is the first member of the next turn; a non-corrective body leaves `AwaitingCorrectionStart`. Prior waiting candles remain excluded, and no body-size threshold is inferred. |
| `BULLISH_CORRECTION_END` | `CONFIRMED` | The correction ends only when a formally closed candle has `Close > prior HH structural level`. |
| `BULLISH_CORRECTION_WINDOW` | `PARTIALLY_DEFINED` | Pre-start waiting exclusion, in-range continuity including non-confirming prior-HH equality/wick-only candles, ordinary strict new-High reset, confirmation-plus-extension validation and exact-body next-turn membership, prior-HL invalidation exclusion and reset/invalidation precedence are confirmed; near-doji treatment, initial-anchor selection and historical scan termination remain unresolved. |
| `PRIOR_HH_EQUALITY_MEMBERSHIP` | `CONFIRMED_INCLUDED` | `High == prior structural HH` or `Close == prior structural HH` does not confirm; absent an independent terminal event, the candle remains in the active candidate-HL turn and its OHLC is geometry-eligible. |
| `PRIOR_HH_WICK_ONLY_PENETRATION_MEMBERSHIP` | `CONFIRMED_INCLUDED` | `High > prior structural HH` with `Close <= prior HH` does not confirm; absent an independent reset/invalidation event, the candle remains in the active candidate-HL turn and its OHLC is geometry-eligible. |
| `BULLISH_CORRECTION_TURN_MEMBERSHIP` | `CONFIRMED` | All in-range intermediate candles remain in the same active bullish correction turn until a terminal/reset event; direction changes and consolidation do not create parallel turns. |
| `BULLISH_INTERNAL_COLOR_ALTERNATION_MEMBERSHIP` | `CONFIRMED` | Bearish, bullish and neutral bodies may remain in the same active turn; alternation alone does not fragment it. |
| `BULLISH_DOJI_INSIDE_ACTIVE_CORRECTION` | `CONFIRMED` | Exact doji cannot start a correction but may remain in an already-active bullish correction turn. |
| `BULLISH_CORRECTION_NEW_HIGH_RESET` | `CONFIRMED` | `High > current correction-origin ceiling` resets an active correction when that same closed candle does not also confirm its candidate with `Close > prior structural HH`; a price extreme alone does not validate a structural HH. |
| `CANDIDATE_CONFIRMATION_AND_ORIGIN_RESET_PRECEDENCE` | `CONFIRMED` | Bullish `Close > prior structural HH` plus `High > current correction-origin ceiling` validates the old candidate HL; generic reset cannot discard it. Its old turn closes successfully. |
| `BEARISH_CANDIDATE_CONFIRMATION_AND_ORIGIN_RESET_PRECEDENCE` | `CONFIRMED` | Bearish `Close < prior structural LL` plus `Low < current correction-origin floor` validates the old candidate LH; generic reset cannot discard it. Its old turn closes successfully. |
| `CONFIRMING_CANDLE_ESTABLISHES_NEW_EXTREME` | `CONFIRMED` | In either compound event, the confirming candle is excluded from the old candidate turn while its upper/lower extreme supplies the new HH/LL-side reference under existing structural-extreme semantics. |
| `CONFIRMED_CANDIDATE_ACTIVE_PAIR_ROLLOVER` | `CONFIRMED` | From the causal bullish/bearish confirming close, the active pair becomes new HH + validated HL or new LL + validated LH; older structure remains in the audit trail. |
| `CONFIRMATION_RESET_CANDLE_NEXT_TURN_MEMBERSHIP` | `CONFIRMED_INCLUDED_WHEN_CORRECTIVE_BODY` | In either compound event, the confirming candle is excluded from the old turn and included exactly once as the first member of the next turn only when its exact body is corrective; otherwise the next turn is empty and the valid side is `AwaitingCorrectionStart`. |
| `CANDIDATE_CONFIRMATION_AND_INVALIDATION_COLLISION` | `NOT_REACHABLE_IN_VALID_STRUCTURE` | Valid bullish `prior validated HL < prior structural HH` and bearish `prior structural LL < prior validated LH` make simultaneous strict confirmation and opposite-point invalidating closes impossible; wick penetration is not body-close confirmation. |
| `BULLISH_CANDIDATE_RESET_ON_NEW_HIGH` | `CONFIRMED` | Without a simultaneous strict confirming `Close > prior HH`, a new-High reset discards the old candidate HL and subsequent search proceeds from the updated ceiling. The confirmed confirmation-plus-extension collision instead validates that candidate. |
| `BULLISH_PRIOR_HL_BODY_CLOSE_INVALIDATION` | `CONFIRMED` | A formally closed `Close < prior validated HL` invalidates bullish structure and terminates pursuit of the same candidate HL. |
| `BULLISH_PRIOR_HL_WICK_ONLY_INVALIDATION` | `CONFIRMED_NO_INVALIDATION` | Equality or `Low < prior HL` with `Close >= prior HL` does not invalidate under the prior-HL body-close rule. |
| `BULLISH_NEW_HIGH_RESET_CANDLE_DUAL_ROLE` | `CONFIRMED` | When the same candle does not confirm the old candidate, a strict new-High candle resets the old correction and updates its ceiling, then closed body direction determines whether it starts a new correction. Confirmation plus extension instead validates the old candidate; exact corrective body determines next-turn inclusion. |
| `BULLISH_RESET_BEARISH_CANDLE_NEW_TURN_MEMBERSHIP` | `CONFIRMED_INCLUDED` | A non-confirming reset candle with `Close < Open` is excluded from the old turn and included as the first member of the new bullish-correction turn; this does not classify a candidate-confirming candle. |
| `BULLISH_RESET_NEUTRAL_CANDLE_CORRECTION_START` | `CONFIRMED_NO_START` | A reset candle with `Close == Open` updates the ceiling but does not start the new correction. |
| `BULLISH_RESET_BULLISH_CANDLE_CORRECTION_START` | `CONFIRMED_NO_START` | A reset candle with `Close > Open` updates the ceiling but does not start the new correction. |
| `BULLISH_INVALIDATION_CANDLE_PREVIOUS_TURN_MEMBERSHIP` | `CONFIRMED_EXCLUDED` | The causal `Close < prior validated HL` candle is excluded from the destroyed bullish correction turn. |
| `BULLISH_INVALIDATION_CANDLE_PREVIOUS_CANDIDATE_GEOMETRY` | `CONFIRMED_EXCLUDED` | The invalidation candle cannot alter the previous candidate's structural price or protection anchor. |
| `BULLISH_INVALIDATION_CANDLE_NEW_BEARISH_IMPULSE_ROLE` | `CONFIRMED` | The invalidation candle supplies new bearish-impulse evidence but is not thereby a validated structural LL. |
| `BULLISH_RESET_INVALIDATION_COLLISION_PRECEDENCE` | `CONFIRMED_INVALIDATION_DOMINATES` | When one closed candle has both `High > current correction-origin ceiling` and `Close < prior validated HL`, both facts are observed but invalidation governs the transition into bearish context. |
| `BULLISH_COLLISION_UPPER_EXTREME_ROLE` | `CONFIRMED` | The collision candle's `High` remains a relevant upper price extreme and bearish-origin reference, with its upper wick serving as protection-reference evidence for the new bearish context; it is not automatically a validated LH, structural High or executable Stop Loss, and the executable offset remains unresolved. |
| `BULLISH_COLLISION_CANDLE_PREVIOUS_TURN_MEMBERSHIP` | `CONFIRMED_EXCLUDED` | The collision candle is excluded from the destroyed bullish correction turn. |
| `BULLISH_COLLISION_CANDLE_PREVIOUS_CANDIDATE_GEOMETRY` | `CONFIRMED_EXCLUDED` | The collision candle cannot retrospectively alter the destroyed candidate HL's `StructuralPrice` or `ProtectionAnchor`. |
| `BULLISH_COLLISION_CANDLE_NEW_BEARISH_IMPULSE_ROLE` | `CONFIRMED` | From its causal close, the collision candle belongs to the new bearish impulse context without automatically validating an LL or LH. |
| `BULLISH_COLLISION_STRUCTURAL_CHANGE_ROLE` | `CONFIRMED` | The strict close below the prior validated HL is causal evidence of the bullish-to-bearish context change; it does not itself satisfy any separately defined Step-3 or Step-4 rule. |
| `BEARISH_RESET_INVALIDATION_SYMMETRY` | `CONFIRMED_MIRROR` | Direct human evidence confirms that active-bearish correction membership, new-Low reset, prior-LH invalidation and collision precedence mirror the bullish mechanics causally. |
| `BEARISH_CORRECTION_TURN_MEMBERSHIP` | `CONFIRMED` | All in-range intermediate candles remain in one active bearish correction turn until reset or invalidation; internal oscillation and consolidation do not create parallel turns. |
| `BEARISH_INTERNAL_COLOR_ALTERNATION_MEMBERSHIP` | `CONFIRMED` | Bullish, bearish and neutral bodies may remain in the same active bearish correction; body-color alternation alone does not fragment it. |
| `BEARISH_DOJI_INSIDE_ACTIVE_CORRECTION` | `CONFIRMED` | Exact doji remains part of an already-active bearish correction when no reset or invalidation occurs. |
| `BEARISH_CORRECTION_NEW_LOW_RESET` | `CONFIRMED` | `Low < current correction-origin floor` resets the active bearish correction when that same closed candle does not also confirm its candidate with `Close < prior structural LL`; a price extreme alone does not validate a structural LL. |
| `BEARISH_CANDIDATE_LH_RESET_ON_NEW_LOW` | `CONFIRMED` | Without a simultaneous strict confirming `Close < prior LL`, a new-Low reset discards the old candidate LH; subsequent search uses the updated floor. The confirmed compound collision instead validates that candidate. |
| `BEARISH_NEW_LOW_RESET_CANDLE_DUAL_ROLE` | `CONFIRMED` | When the same candle does not confirm the old candidate, a strict new-Low candle resets the old correction and updates its floor, then exact body direction determines whether it starts a new correction. Confirmation plus extension instead validates the old candidate; exact corrective body determines next-turn inclusion. |
| `BEARISH_RESET_BULLISH_CANDLE_NEW_TURN_MEMBERSHIP` | `CONFIRMED_INCLUDED` | A non-confirming reset candle with `Close > Open` is excluded from the old turn and included as the first member of the new bearish-correction turn; compound candidate confirmation follows its separate rule. |
| `BEARISH_RESET_NEUTRAL_CANDLE_CORRECTION_START` | `CONFIRMED_NO_START` | A reset candle with `Close == Open` updates the floor but starts no new bearish correction. |
| `BEARISH_RESET_BEARISH_CANDLE_CORRECTION_START` | `CONFIRMED_NO_START` | A reset candle with `Close < Open` updates the floor but starts no new bearish correction. |
| `BEARISH_PRIOR_LH_BODY_CLOSE_INVALIDATION` | `CONFIRMED` | A formally closed `Close > prior validated LH` invalidates bearish structure and terminates pursuit of the same candidate LH. |
| `BEARISH_PRIOR_LH_WICK_ONLY_INVALIDATION` | `CONFIRMED_NO_INVALIDATION` | Equality or `High > prior LH` with `Close <= prior LH` does not invalidate under the strict body-close rule. |
| `BEARISH_INVALIDATION_CANDLE_PREVIOUS_TURN_MEMBERSHIP` | `CONFIRMED_EXCLUDED` | The causal invalidation candle is excluded from the destroyed bearish correction turn. |
| `BEARISH_INVALIDATION_CANDLE_PREVIOUS_CANDIDATE_GEOMETRY` | `CONFIRMED_EXCLUDED` | The invalidation candle cannot retrospectively alter the candidate LH's `StructuralPrice` or `ProtectionAnchor`. |
| `BEARISH_INVALIDATION_CANDLE_NEW_BULLISH_IMPULSE_ROLE` | `CONFIRMED` | The invalidation candle belongs to the new bullish impulse context without automatically validating an HL or HH. |
| `BEARISH_RESET_INVALIDATION_COLLISION_PRECEDENCE` | `CONFIRMED_INVALIDATION_DOMINATES` | When one closed candle has both `Low < current correction-origin floor` and `Close > prior validated LH`, both facts remain observed but invalidation governs the transition. |
| `BEARISH_COLLISION_LOWER_EXTREME_ROLE` | `CONFIRMED` | The collision candle's `Low` remains a lower price extreme, bullish-origin reference and wick-based protection evidence; it is not automatically a validated HL, Structural Low or executable Stop Loss. |
| `BEARISH_COLLISION_CANDLE_PREVIOUS_TURN_MEMBERSHIP` | `CONFIRMED_EXCLUDED` | The collision candle is excluded from the destroyed bearish correction turn. |
| `BEARISH_COLLISION_CANDLE_PREVIOUS_CANDIDATE_GEOMETRY` | `CONFIRMED_EXCLUDED` | The collision candle cannot alter the destroyed candidate LH's prior `StructuralPrice` or `ProtectionAnchor`. |
| `BEARISH_COLLISION_CANDLE_NEW_BULLISH_IMPULSE_ROLE` | `CONFIRMED` | From its causal close, the collision candle belongs to the new bullish impulse context without automatically validating an HL or HH. |
| `CANDIDATE_HL_PRICE_SELECTION` | `PARTIALLY_DEFINED` | Within the selected correction/turn, the deepest candidate's lowest body `Open`/`Close` determines the price; exact equal-coordinate identity is resolved, while remaining turn boundaries and near-equal tolerance stay open. |
| `CANDIDATE_HL_IDENTITY_SELECTION` | `CONFIRMED_PRICE_LEVEL` | Structural identity is the resulting price level; no individual candle/timestamp owns an exact equal-price HL. |
| `STRUCTURAL_EQUAL_PRICE_IDENTITY` | `CONFIRMED_PRICE_LEVEL` | Two or more selected-turn candles at the exact structural body coordinate represent one structural price level. |
| `STRUCTURAL_EQUAL_PRICE_CANDLE_TIE_BREAK` | `CONFIRMED_NOT_REQUIRED` | Exact equality requires no first/last candle, timestamp or sequence-number priority. |
| `STRUCTURAL_EQUAL_PRICE_SHARED_ZONE` | `CONFIRMED` | Multiple exact-equal bodies may support one horizontal level/zone without creating multiple HL/LH points; no numeric zone width is implied. |
| `STRUCTURAL_PROTECTION_WICK_ACROSS_TURN` | `CONFIRMED` | Protection uses minimum `Low` for a lower/HL turn or maximum `High` for an upper/LH turn across all selected members, independently of the body-based structural price. |
| `NEAR_EQUAL_STRUCTURAL_ZONE_TOLERANCE` | `UNRESOLVED` | No epsilon, tick/point distance, percentage band or fuzzy comparison merges different structural body prices. |
| `CANDIDATE_LH_SELECTION` | `CONFIRMED` | Within one recognized bearish correction, retain the highest candidate high and replace lower intermediate candidates; causal validation and the selected-turn body coordinate remain governed by their separate confirmed rules. |
| `COMPLEX_CANDIDATE_LH_SELECTION` | `CONFIRMED` | Within one recognized bearish correction, the highest candidate high is selected; superseded intermediate highs do not survive. |
| `HIGHEST_HIGH_REPLACEMENT` | `CONFIRMED` | A later higher candidate high replaces the earlier lower candidate before the causal closed break below the prior LL. |
| `BEARISH_CORRECTION_WINDOW` | `PARTIALLY_DEFINED` | Pre-start waiting exclusion, in-range membership including non-confirming prior-LL equality/wick-only candles, ordinary strict new-Low reset, same-candle bullish restart, prior-LH invalidation, reset/invalidation precedence, causal `Close < prior LL` end, confirmation-plus-extension validation and exact-body next-turn membership are confirmed; near-doji treatment, initial-anchor selection and historical scan termination still prevent a complete detector. |
| `PRIOR_LL_EQUALITY_MEMBERSHIP` | `CONFIRMED_INCLUDED` | `Low == prior structural LL` or `Close == prior structural LL` does not confirm; absent an independent terminal event, the candle remains in the active candidate-LH turn and its OHLC is geometry-eligible. |
| `PRIOR_LL_WICK_ONLY_PENETRATION_MEMBERSHIP` | `CONFIRMED_INCLUDED` | `Low < prior structural LL` with `Close >= prior LL` does not confirm; absent an independent reset/invalidation event, the candle remains in the active candidate-LH turn and its OHLC is geometry-eligible. |
| `PRIOR_HH_LL_NON_CONFIRMING_BOUNDARY_MEMBERSHIP` | `CONFIRMED_INCLUDED` | Touch, equality and wick-only penetration of the prior structural validation HH/LL alone neither confirms nor ends an active candidate turn. The distinct origin ceiling/floor reset and opposite-point invalidation rules still take precedence when independently satisfied. |
| `CANDIDATE_LH_STRUCTURAL_PRICE` | `CONFIRMED` | For an already selected LH turn, use the highest `Open`/`Close` body edge among its member candles; candle color is non-normative and a higher wick is excluded. |
| `BEARISH_STRUCTURAL_BODY_ANCHOR` | `CONFIRMED` | The structural LH line is anchored where candle bodies stop, at the selected turn's highest body edge. |
| `CORRECTION_PHASE_BOUNDARIES` | `PARTIALLY_DEFINED` | Exact directional starts, exclusion of every pre-start waiting candle, non-confirming prior-HH/LL boundary inclusion, same-candle origin-extreme precedence, causal confirmations and mirrored terminal membership are confirmed; near-doji treatment beyond exact comparisons, initial-anchor selection and historical scan termination remain undefined. |
| `TURN_MEMBERSHIP_SEGMENTATION` | `PARTIALLY_DEFINED` | Both directions have confirmed pre-start exclusion, active-turn equality/wick-only prior-HH/LL inclusion, ordinary reset/start inclusion, invalidation exclusion and confirming-candle exclusion from the old turn; a confirming/extending candle joins only the next turn when its exact body is corrective. Near-doji treatment, initial-anchor selection and historical scan termination remain unresolved for broader reconstruction. |
| `STRUCTURAL_BODY_ANCHOR` | `PARTIALLY_DEFINED` | For a supplied candidate-HL turn, use the lowest `Open` or `Close` among its bodies; terminal membership is defined, but remaining turn edges and other structural classes prevent a complete selector. |
| `BODY_ANCHOR_TIE_BREAKING` | `CONFIRMED_NOT_REQUIRED` | Exact equal body coordinates yield one price level; candle ownership is not part of the structural result. |
| `EXACT_DOJI_CORRECTION_START` | `CONFIRMED_NO_START` | A candle with `Close == Open` does not start either directional correction; wait for the next qualifying directional-body candle. |
| `DOJI_DIRECTIONAL_ROLE` | `CONFIRMED_NEUTRAL` | Exact equality supplies neither bullish nor bearish body direction and cannot be reclassified using later data or another timeframe. |
| `DOJI_WITH_NEW_HIGH_PRECEDENCE` | `CONFIRMED` | An exact doji may update the correction-origin ceiling with a new `High`, but the correction remains not started. |
| `DOJI_WITH_NEW_LOW_PRECEDENCE` | `CONFIRMED` | An exact doji may update the correction-origin floor with a new `Low`, but the correction remains not started. |
| `NEAR_DOJI_THRESHOLD` | `UNRESOLVED` | No epsilon, tick distance or body-size threshold defines a near-doji; only exact `Close == Open` is covered. |
| `DOJI_BOUNDARY_BEHAVIOR` | `PARTIALLY_DEFINED` | Exact doji behavior is confirmed; near-doji/small-body classification remains unresolved. |
| `SELL_LH_SL_ANCHOR` | `CONFIRMED` | For sell risk management, Stop Loss is above the highest wick/tail of the relevant LH; this is not a structural target/reconstruction coordinate. |
| `STRUCTURAL_IDENTITY_VS_PRICE_SEPARATION` | `CONFIRMED` | LH identity selects the swing, the highest selected-turn body edge supplies its structural coordinate, and the separate sell SL anchor remains above the highest wick/tail. |
| `CONFIRMING_CANDLE_CORRECTION_MEMBERSHIP` | `CONFIRMED_EXCLUDED` | The causal break candle is excluded from the candidate turn it validates; it may instead be the first member of the *next* turn when its exact body is corrective. |
| `CONFIRMING_CANDLE_VALIDATION_ROLE` | `CONFIRMED` | Its formally observable close confirms the new HH/LL, ends the correction, validates the preceding candidate and establishes the causal `AsOfUtc`. |
| `IMPULSE_LEG_MEMBERSHIP` | `PARTIALLY_DEFINED` | Expansion toward the causal break, including the confirming candle's validation role, is outside the old correction; a confirming/extending candle with a corrective body can also start the next turn. Other turn/leg segmentation remains unresolved. |
| `SINGLE_CANDLE_BREAK_ORIGIN_CASE` | `CONFIRMED` | One expansive candle may contain impulse-origin evidence and also produce the causal confirming close without becoming a member of the old candidate turn. |
| `CONFIRMING_CANDLE_SCAN_INCLUSIVITY` | `CONFIRMED_EXCLUDED` | The confirming candle is excluded from the old correction-candidate scan; its origin evidence is retained separately in the single-candle case, and next-turn inclusion follows the compound rule. |
| `CONFIRMING_BODY_CLOSE_CANDLE_MEMBERSHIP` | `CONFIRMED_EXCLUDED` | A formally closed `Close > prior structural HH` or `Close < prior structural LL` supplies causal candidate validation but is excluded from the preceding correction/candidate turn and cannot change its selected-turn `StructuralPrice` or `ProtectionAnchor`. |
| `SINGLE_CANDLE_ORIGIN_PRICE_COORDINATE` | `CONFIRMED` | For the single-candle bullish HL or bearish LH origin, use that candle's `Open`; its wick does not redefine the structural coordinate. |
| `SINGLE_CANDLE_HL_STRUCTURAL_PRICE` | `CONFIRMED` | A single bullish origin/confirming candle supplies `structuralHlPrice = Candle.Open` once its causal close is observable. |
| `SINGLE_CANDLE_LH_STRUCTURAL_PRICE` | `CONFIRMED` | A single bearish origin/confirming candle supplies `structuralLhPrice = Candle.Open` once its causal close is observable. |
| `BUY_HL_PROTECTION_ANCHOR` | `CONFIRMED` | Buy protection references the relevant HL `Low`/lowest wick extreme; this is not the structural origin coordinate or executable Stop Loss price. |
| `SELL_LH_PROTECTION_ANCHOR` | `CONFIRMED` | Sell protection references the relevant LH `High`/highest wick extreme; this is not the structural origin coordinate or executable Stop Loss price. |
| `ACTUAL_SL_OFFSET_FROM_ANCHOR` | `UNRESOLVED` | “Below”/“above” the protection anchor does not define buffer, ticks, spread/cost adjustment or broker order price. |
| `STRUCTURE_VS_PROTECTION_SEPARATION` | `CONFIRMED` | Single-candle structure uses `Open`; buy/sell protection uses `Low`/`High`, with executable offset handled separately. |
| `STRUCTURAL_CANDIDATE_DETECTION` | `PARTIALLY_DEFINED` | Exact equal-price identity, pre-start exclusion, mirrored non-confirming prior-HH/LL inclusion, confirming-candle exclusion from the old turn, ordinary reset/invalidation precedence, both confirmation-plus-extension cases and ordered-reference impossibility of confirmation plus invalidation are confirmed; near-equal tolerance, near-doji treatment, initial-anchor selection and historical scan termination remain non-reproducible for broader detection. |
| `EXACT_STRUCTURAL_PRICE_COORDINATE` | `PARTIALLY_DEFINED` | Candidate-HL and candidate-LH selected-turn body anchors are defined and bullish terminal membership constrains the supplied turn, but coordinates for other structural-point classes remain unresolved. |

### 9.3 Breakout

`Breakout` is conceptually `confirmed`. The reviewed 4H evidence at approximately `02:20–02:40` requires a candle-body close beyond the relevant prior structural level or zone; a wick alone does not confirm it. For a bullish context, the 4H candle closes above relevant structural resistance, which may be the prior HH. For a bearish context, it closes below relevant structural support.

The exact support/resistance selection, zone construction and width, tolerance, choice among multiple levels and any minimum penetration or confirming-candle count remain unresolved. Consequently, the complete Breakout detector is not deterministic.

### 9.4 Wickfill

`Wickfill` is conceptually `confirmed` and operationally `human_validation_required`. At approximately `04:23–04:32`, the mentor describes it as filling the wick/tail. The confirmed progression is: a Breakout has occurred, the extension leaves a wick/extreme, price moves away or retraces, and a later impulse seeks to travel through that space again and reach or exceed the earlier wick extreme. The example at approximately `06:55–07:25` shows a retracement, ideally toward support/old resistance, followed by that renewed impulse.

The source does not yet establish whether touching or exceeding the extreme completes Wickfill, whether a close is required, which wick applies when several exist, the exact retracement requirement, the support/resistance selection or tolerance. No exact comparison is inferred from “reach or exceed.”

### 9.5 Fakeout

`Fakeout` is conceptually `confirmed` and operationally `human_validation_required`. At approximately `10:40`, it is described as a Breakout that did not complete. The reviewed example at approximately `10:55–11:25` confirms this progression: price exits a relevant support, resistance, range or structural zone; the break fails; price returns; and a new candle closes back inside the relevant zone. A wick returning inside is insufficient when no candle has closed back inside.

The allowed time/candle count, penetration tolerance, exact zone width and selection, which returning candle applies, numeric invalidation and effect on bias remain unresolved.

### 9.6 Context relationship and implementation boundary

At the 08:00 review, `Breakout`, `Wickfill` and `Fakeout` are three alternative current context/scenario classifications: `Breakout OR Wickfill OR Fakeout`, not an `AND` gate. They can nevertheless have causal history: Wickfill may follow an extended Breakout, while Fakeout is a failed Breakout that closes back inside structure. The source does not define formal mutual exclusivity or precedence when visual conditions appear to coexist, so classification remains human-validated.

These structural and context relationships are confirmed strategy concepts, not a complete swing, pivot or scenario-classification engine. No fractal, fixed pivot width, ZigZag, percentage/ATR swing, point threshold, nearest local extreme or fixed lookback is part of the audited rule.

## 10. Liquidity

Mark Asia High, Asia Low, London High and London Low. Also review structural points on 1H/4H, especially when they coincide with those session extrema. These horizontal liquidity references are a separate step and do not bootstrap 4H HH/HL/LL/LH structure. Coincidence may make a level more relevant in the mentor's reading, but no automatic hierarchy is defined. A liquidity level must be taken before searching for 5M inversion; the take alone is never an entry.

For each trading/preparation day `D`, session membership is defined in local `America/Bogota` time by the 1H candle `OpenTime`:

| Session | Interval | Included nominal 1H opens | Nominal count sanity check |
|---|---|---|---:|
| Asia | `[D-1 17:00, D 02:00)` | 17:00 through 23:00 on `D-1`, then 00:00 and 01:00 on `D` | 9 |
| London | `[D 02:00, D 07:00)` | 02:00 through 06:00 on `D` | 5 |

Intervals are start-inclusive and end-exclusive. Asia crosses midnight; London does not within this definition. Therefore, the 02:00 candle belongs to London, not Asia, and the 07:00 candle does not belong to London. The nominal intervals have neither overlap nor gap. The candle counts are sanity checks for complete, hourly-aligned data; they do not authorize synthesis of missing candles or assumptions about weekends, holidays or early closes.

`NQ-LIQ-001` uses only completed, closed 1H candles whose local `OpenTime` belongs to the corresponding interval:

- `AsiaHigh(D) = maximum Candle.High` among the relevant completed Asia candles.
- `AsiaLow(D) = minimum Candle.Low` among the relevant completed Asia candles.
- `LondonHigh(D) = maximum Candle.High` among the relevant completed London candles.
- `LondonLow(D) = minimum Candle.Low` among the relevant completed London candles.

These extrema do not use `Open`, `Close`, candle bodies, averages, pivots or future candles. No resampling, interpolation, forward fill or synthetic candle generation is permitted. Missing required session data must produce `data_unavailable`; a future implementation must define and test provider-specific data completeness without inventing calendar behavior. At the normal 08:00 `America/Bogota` preparation time, Asia has ended at 02:00 and London at 07:00, so all four references come from completed prior sessions; this observation does not add a new execution rule.

These definitions make `NQ-LIQ-001` deterministically specified; this documentation change does not alter its existing runtime evaluator. Equal relevant Asia/London prices can be compared deterministically and collapse into one target. If a relevant session target is no longer a future objective before the operational scenario, the source uses structural fallback from Step 2. The fallback candidate classes, causal validation rule, selected-turn HL/LH body coordinates and reviewed 1H-first/4H-extended relationship are source-supported, but initial candidate detection, correction-edge semantics, exact mitigation, other structural-point coordinates, PDH/PDL ranking, coincidences, ties and reuse validity remain unresolved or require human validation. The post-08:30 edge is resolved: the first valid take is Step 3, subject to Step-1 4H alignment, and does not use fallback to save the prior imagined scenario. Multiple-take setup association is direction-dependent and resolved in section 6.2. The confirmed high-level take condition is only that price exceeds an identified High or goes below an identified Low; no extra penetration threshold, point/tick count, close-back, rejection, displacement or tolerance is inferred. The reviewed use of "touch/take" does not establish equality-only Step-3 activation, so Step-3 geometry remains `CONFIRMED_EXCEEDS` and stays distinct from exact selected-target touch.

Directional source examples support waiting for downside liquidity such as an Asia/London Low before bullish continuation, and upside liquidity such as an Asia/London High before bearish continuation. They do not define automatic priority among Asia, London or structural liquidity references.

## 11. Operating schedule

```yaml
preparation_start_time: "08:00"
entry_start_time: "08:30"
trading_window_end_time: "11:00"
timezone: "America/Bogota"
daylight_saving_adjustment: false
```

Preparation/analysis may start at 08:00, and same-session Steps 1 and 2 must both be complete in `[08:00:00, 08:30:00)`. Completion at 08:29:59.xxx satisfies timing; 08:30:00 is the phase boundary and is too late. The operational entry-acquisition window is `[08:30, 11:00)`: it starts at 08:30 only for a prepared session and ends when 11:00 is reached. All three times are local Colombia times governed normatively by `America/Bogota`, which does not apply seasonal DST adjustments to these limits. Reaching the clock boundary without evidence of both preparation steps does not establish completion.

The strategy operates the New York market, but that market reference does not change its clock to `America/New_York`, EST or EDT. The same confirmed timezone governs the Asia and London definitions in section 10. Allowed days, holidays, early closes, low-liquidity sessions and management of positions after a pre-11:00 entry remain unresolved. No forced exit at 11:00 is confirmed.

## 12. 5M inversion

Only after the required liquidity take, two Step-4 alternatives are valid with `OR`: traditional Structural Change/ChoCH/MSS or IFVG, whichever occurs first. Both alternatives are not required and neither has a fixed priority. The extreme left by the liquidity-taking event can serve as the first 5M High/Low reference from which the mentor begins reading subsequent 5M structure; this observation does not define a complete 5M structural detector and does not solve 4H bootstrap.

Until Step 4 is confirmed and while local time remains before 11:00, a valid farther same-side extreme replaces the current active 5M structural reference within the same single setup; Step 3 remains the last satisfied gate and prior observations remain in the audit history. Continued movement in the manipulation direction and a wick-only structural break do not confirm Step 4 and do not alone cancel the setup. Separately, exact wick contact with the first encountered opposite-side Asia/London target cancels the setup before completed pre-entry progression. The setup expires at 11:00 under a distinct time-based condition. The source does not define branch-specific IFVG reset or reference-replacement behavior.

- Traditional change — for buys, break bearish structure; for sells, break bullish structure. A strong/decisive 5M candle-body close beyond the relevant prior swing is required. A wick does not confirm. “Strong/decisive,” pivot selection and marginal-close thresholds require human validation and have no numeric threshold.
- IFVG — conceptually `confirmed`: candle-body action invalidating a prior FVG in the opposite direction may replace traditional structural change. Geometry, direction, partial/full close, mitigation, confirming candle, expiry and displacement relation are unresolved. No external IFVG definition applies. A Step-4 IFVG is not a direct entry and does not automatically satisfy Step 5.

## 13. 5M continuation FVG

```yaml
required: true
candle_count: 3
minimum_size_points: null
quality_validation: human_validation_required
```

This is a separate mandatory gate after the Step-4 trigger. The displacement responsible for the new direction must leave a 5M FVG favoring that move; without it, Step 6 is not enabled. A Step-4 IFVG does not satisfy this requirement merely by being an IFVG.

The previously audited specification describes the FVG as a three-candle space between the first and third candle wicks. One or two points were described as insufficient evidence of strength, but this qualitative observation does not establish a three-point minimum. Clear/evident size, volatility relation, fills, invalidation, lifespan, multiple-FVG selection and whether any Step-4 IFVG can also form a distinct Step-5 FVG remain open.

## 14. 1M entry

Only after the mandatory Step-5 FVG, move to 1M. Wait for a countertrend retracement toward/into that relevant 5M FVG and then for 1M structural realignment with the intended direction. For sells, the pullback is bullish and the realignment bearish; for buys, the pullback is bearish and the realignment bullish. Reaching the 5M FVG alone does not authorize entry. The last corrective swing must break with a candle-body close in the intended direction; if 1M never realigns, no entry concept becomes eligible.

### Entry swing

The last swing of the corrective 1M structure whose break and close enables entry.

### Earlier post-entry swing observation

Earlier evidence distinguished a post-entry management swing from the corrective entry swing. The authoritative Break-Even evidence now uses the first encountered favorable Asia/London session target instead; whether the earlier swing retains a separate role remains unresolved and it is not the canonical trigger in this specification.

Order type, close-versus-next-open timing, slippage, maximum entry distance, attempts, reentries and corrective-swing algorithm are unresolved. The time boundary is resolved: all pre-entry confirmation must complete before 11:00, and price must not be chased when the intended target was already consumed.

## 15. Stop Loss

Status: conceptually `confirmed`; deterministic geometry remains `human_validation_required`.

```yaml
buy_reference: structural_5m_hl
sell_reference: structural_5m_lh
invalidation_principle: trade_idea_loses_structural_meaning
selection_algorithm: null
buffer_points: null
status: confirmed_concept
human_validation_required: true
```

Manual source-video re-verification resolves the prior competing interpretation involving the sweep extreme. The confirmed protection anchor is the `Low`/lowest wick extreme of the latest relevant structural 5M HL for buys and the `High`/highest wick extreme of the latest relevant structural 5M LH for sells, where the trade idea loses structural meaning. In the single-candle origin case, those wick anchors remain distinct from the structural `Open` coordinate.

This reconciliation does not define how to detect HL/LH, select the latest relevant structural swing or translate “below/above the wick” into an executable price. `ACTUAL_SL_OFFSET_FROM_ANCHOR` remains `UNRESOLVED`: no one-tick, one-point, fixed, percentage, ATR, spread or broker-minimum offset is inferred. Spread, slippage, commission/cost adjustment, maximum Stop Loss, oversized-stop behavior, pre-entry invalidation and risk reduction remain open. Human validation is still required and fully automatic demo execution remains blocked.

## 16. Break Even

The newly confirmed management concept uses favorable session liquidity rather than treating an isolated post-entry swing as the canonical trigger. For a sell, the candidates are London Low and Asia Low; when a price quote touches the first relevant candidate encountered downward, move Stop Loss to entry. For a buy, the candidates are London High and Asia High; when a price quote touches the first relevant candidate encountered upward, move Stop Loss to entry. This event is independent of chart timeframe. Exact contact with the horizontal wick extreme is sufficient without body close, candle-close confirmation, tolerance or penetration buffer. Video 3 approximately `13:20–13:35` shows the mentor observing the touch on a 1M chart, while the fixed target/order level itself is handled by price interaction rather than defined by that chart timeframe.

The relationship between this authoritative trigger and the earlier documented post-entry swing break is unresolved; the earlier swing rule must not replace the first-encountered session-liquidity concept automatically. When the Asia and London target prices coincide, the reviewed scenario uses their one shared level as final Take Profit. It does not create a distinct earlier BE target or a fabricated move-to-BE followed by exit at the same price. The source says to move Stop Loss to entry/Break-Even in the distinct-target case; it does not define BE plus ticks, spread or commission adjustment. Universality, commissions, spread, exact entry/cost basis, later management and whether the first target is final Take Profit outside the reviewed equal-target case remain open. Historical same-candle ordering is a data-resolution limitation, not an unresolved BE timeframe. The strategy concept is `confirmed`; a deterministic Break-Even algorithm is not yet fully defined.

## 17. Take Profit

```yaml
buy_target_reference: first_relevant_asia_or_london_high_encountered_upward
sell_target_reference: first_relevant_asia_or_london_low_encountered_downward
structural_fallback_readiness: partially_defined
importance_algorithm: null
target_priority: first_encountered_in_expected_price_path
fixed_risk_reward: null
partials: null
status: confirmed_concept
human_validation_required: true
```

Manual source-video re-verification confirms the primary session candidates for this role: Asia High and London High for buys, and Asia Low and London Low for sells. When two future-valid levels differ, the immediate target is the first relevant level price encounters in the expected direction of travel. When their prices are equal, they collapse into one unique target; the reviewed equal-target scenario treats that level as final Take Profit. If a relevant session target was already consumed before the operational period or is otherwise unavailable as a future objective, the source uses structural fallback. A sell may target relevant unmitigated support represented by a validated 1H/4H HL or LL, or Previous Day Low. A buy may target relevant unmitigated resistance represented by a validated 1H/4H LH or HH, or Previous Day High. In reviewed paths with distinct relevant levels, 1H is the first objective or management level and the farther 4H level is the final or extended objective; 4H is structurally stronger. This hierarchy is confirmed only within that observed scope and is not a universal formula. Arbitrary structural points do not become coequal primary candidates while valid session targets remain available.

### 17.1 Structural fallback evidence boundary

| Point | Evidence status | Audited boundary |
|---|---|---|
| Sell fallback candidates: relevant unmitigated support at a validated 1H/4H HL or LL | `CONFIRMED` | Candidate classes and causal swing-validation rule are confirmed; initial candidate detection remains human-validated. |
| Buy fallback candidates: relevant unmitigated resistance at a validated 1H/4H LH or HH | `CONFIRMED` | Candidate classes and causal swing-validation rule are confirmed; initial candidate detection remains human-validated. |
| Previous Day Low for sells and Previous Day High for buys | `CONFIRMED` | Inclusion is confirmed; ranking against 1H/4H candidates is not. |
| Distinct 1H first objective/management level followed by farther 4H final/extended objective | `CONFIRMED` | Limited to reviewed paths; not a universal target formula. |
| 4H is structurally stronger than 1H | `CONFIRMED` | Conceptual strength does not by itself select a numeric target. |
| Mark structural support/resistance from candle bodies, not an isolated long wick | `CONFIRMED` | Body-zone concept only. |
| Meaning of relevant and not mitigated | `PARTIALLY_DEFINED` | Directional role is confirmed; exact OHLC mitigation test and threshold are unresolved. |
| Candle/body used to place the line | `PARTIALLY_DEFINED` | A selected candidate-HL turn uses its lowest body `Open`/`Close`, while a selected candidate-LH turn uses its highest body `Open`/`Close`; exact equal coordinates support one price level without candle ownership, while near-equal tolerance and other structural-point coordinates remain unresolved. |
| HL/LL/LH/HH causal validation after a formally closed body break | `CONFIRMED` | Applies only after the confirming close; it cannot validate the candidate retroactively at earlier replay timestamps. |
| Initial HL/LL/LH/HH candidate detection | `PARTIALLY_DEFINED` | Bullish candidate-HL window/deepest-low/base selection, price anchor and mirrored terminal membership are materially narrowed; non-confirming prior-HH/LL boundary inclusion, exact-doji no-start, both same-candle origin-extreme precedence cases, bearish start/end, causal validation, complex highest-candidate selection and selected-turn highest-body-edge coordinate are confirmed. No fixed lookback exists; near-doji treatment, other class selection, first-anchor selection and historical scan termination remain undefined. |
| Reduction of other body zones to one decimal price | `UNRESOLVED` | Selected-turn HL/LH extrema are defined without a tolerance; no coordinate rule is established for other structural-point classes. |
| PDH/PDL priority against 1H/4H candidates | `UNRESOLVED` | No ranking rule is defined when several candidates coexist. |
| Coincidence or ties among PDH/PDL and 1H/4H structure | `UNRESOLVED` | No merge or tie-break rule is defined. |

Exact quote-level touch of an already-selected absolute price is sufficient; no body close, candle close, chart timeframe or tolerance is required. That touch rule does not define how a structural body zone becomes the selected decimal price. One reviewed example around Video 3 `13:25–13:40` supports complete exit when its selected level is touched, and the latest review confirms final-TP treatment for the reviewed coincident-target scenario. Neither establishes that every first distinct target is final TP or defines universal full-exit behavior. Historical same-candle ordering remains limited by market-data resolution. Structural fallback remains `PARTIALLY_DEFINED`; fixed ratio, partials, trailing and other final-target hierarchy remain unresolved source questions. The resolved post-08:30 Step-3 orientation does not change these target-management semantics.

## 18. Risk

```yaml
maximum_risk_per_trade_percent: 1.0
status: confirmed
evidence_timestamp: "50:15"
daily_loss_limit_percent: null
daily_loss_limit_status: candidate
```

The 1% daily loss limit is `candidate` pending manual evidence validation. Account minimum, sizing, points/ticks formula, tick value, costs, trades/losses per day, post-BE risk, risk reduction, kill switch and rejected-order handling are unresolved.

## 19. News

```yaml
observed_news_time: "10:00"
observed_close_time: "09:45"
observed_action: close_all
status: context_specific
universal_close_minutes_before: null
```

This single example does not establish a universal 15-minute rule. Impact definition, calendar, events, before/after windows, open positions, new entries and resumption are unresolved.

## 20. Reentries

```yaml
after_break_even: null
after_stop_loss: null
same_setup: null
independent_new_setup: null
maximum_attempts: null
status: unresolved
```

The phrase similar to “if it takes you out, do not seek re-entry” lacks sufficient scope. It does not confirm “no re-entry after any exit.”

## 21. Waiting states

Existing audited status semantics remain: return `waiting` before 08:30 `America/Bogota`, while an expected event is pending, `data_unavailable` for missing required closed candles/timestamps and `human_validation_required` at subjective gates. The new strict dependency model does not by itself decide whether an unmet prerequisite maps to `failed`, `waiting` or `not_applicable`; that runtime mapping remains to be audited separately. Regardless of status label, downstream eligibility is not established while a mandatory prior gate is absent.

## 22. Entry-ineligibility conditions

Confirmed:

- No entry without a liquidity sweep.
- No entry without confirmed 5M inversion.
- A wick is not a 5M structural change.
- No entry eligibility without a continuation 5M FVG.
- FVG strength must be approved by human review before downstream eligibility.
- No entry without 1M retracement and realignment.
- No entry before 08:30 `America/Bogota`.
- No new or unfinished pre-entry setup may continue at or after 11:00 `America/Bogota`.
- No entry when the first encountered intended Asia/London target was touched before completed pre-entry progression; exact wick contact is sufficient and no close or tolerance applies. Do not chase price.
- No automatic direction reversal after opposite-side target consumption; a new Step 3 requires alignment with the Step-1 4H permitted direction.
- No revival of a cancelled setup through later 5M or 1M evidence.

These dependency statements do not select a runtime final verdict. FVG size/quality, displacement, Wickfill/Fakeout, structural HL/LH selection and important-high/important-low selection require human validation. News, oversized stops, target distance, reentries, operation counts, daily limit, data quality, lateral markets and holidays remain unresolved.

## 23. Human-validation points

Human validation is required for 4H initial-anchor selection and historical scan termination, delimiting the relevant retracement, translating body-marked structural points into exact prices, Breakout/Wickfill/Fakeout geometry/classification precedence, liquidity selection for Step 3, structural fallback detection, mitigation and PDH/PDL ranking, 5M pivots, “strong/decisive” close, IFVG, FVG quality/lifecycle, 1M FVG interaction/corrective swing, order mechanics, structural Stop Loss selection/offset, unresolved target edges, general final-TP behavior, sizing, daily limit, news and reentries. The confirmed absence of a fixed lookback, direction from the latest confirmed break, active-pair compression, evidence-bounded 1H-first/4H-extended relationship and body-over-isolated-wick marking principle do not remove that validation requirement. Timezone/DST interpretation, the 08:00 closed-4H filter, rejection of minor fluctuations, mandatory step order, timeframe-independent target touch semantics, equality collapse and confirmed body-close/retrospective-confirmation sequence are not human-validation points. Same-candle ordering with OHLC remains a data limitation and has no inferred runtime mapping.

## 24. Automation readiness

The evidence supports assisted analysis, manual backtesting and supervised paper trading. Semi-automatic backtesting remains partial. The end-to-end gate order, trading window, Asia/London boundaries and session-extrema calculation are confirmed; Step-1 and Step-2 inputs remain explicitly separate. The first valid post-08:30 take as Step 3, mandatory Step-1 4H alignment, same-side reference replacement and opposite-side active-target cancellation are source-supported concepts. Runtime supports generic lifecycle transitions and Nasdaq time expiration, but target-consumption behavior and the newly reconciled Step-3 orientation are not implemented. Canonical historical replay exposes closed OHLC `CandleSeries` and can optionally expose normalized high-resolution price observations through `StrategyReplayContext`; the generic deterministic price-level touch primitive consumes an already-selected target but does not select targets or alter lifecycle. Candle evidence can establish occurrence within an interval, while high-resolution evidence can establish an exact source timestamp when supplied. Unsupported same-observation ordering remains a market-data limitation. Distinct and equal session-target semantics are source-defined. Structural fallback remains `PARTIALLY_DEFINED`: candidate classes, both directional correction starts and causal swing-validation ends, exact-doji neutral/no-start behavior, both same-candle origin-extreme precedence cases, mirrored reset/invalidation and collision terminal membership, the bearish prior-LL origin, confirmed complex candidate-LH selection/highest-high replacement, selected-turn highest-body-edge LH coordinate, the partially defined correction windows, candidate-HL deepest-low/base selection and selected-turn lowest-body price, plus the bounded 1H-first/4H-extended relationship, are source-supported. Near-doji thresholds, other structural-point coordinates, mitigation and PDH/PDL ranking remain source gaps. The sell highest-wick/tail Stop Loss anchor remains separate risk evidence, not the body-based target coordinate. General final-TP behavior also remains unresolved.

## 25. Traceability requirements

Record at minimum: `Strategy`, `Strategy version`, `Instrument`, `Data source`, `Timestamp`, `Timezone`, `Market snapshot`, `Rules evaluated`, `Rule statuses`, `Evaluation results`, `Evidence`, `Evidence timestamp`, `Final verdict`, `Human validations`, `Entry`, `Stop Loss`, `Take Profit`, `Result`, `Failure classification` and `Trace identifier`. Preserve candle-close ordering and exclude future data.
